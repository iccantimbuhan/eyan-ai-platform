import { ComingSoon } from '@/components/coming-soon'

// Sprint 0 (ADR-0025/ADR-0026) — placeholder only. Proves the nav/module/
// permission gating chain end-to-end before any real Restaurant Operations
// feature exists. Sprint 1+ replaces this page's contents without touching
// the route file that points at it.
export function RestaurantDashboardPage() {
  return <ComingSoon />
}
