# Project State

_Last updated: 2026-08-01 (AI Core Foundation — Phase 1) by Claude Code_

This file contains **only the current state** of the project. It is overwritten at the end of every phase/session — it is not a log. For history, follow the links in Pointers below.

---

## Current Sprint

**AI Core Foundation — Phase 1 — COMPLETE.** A new platform module, architecturally MCP Foundation's sibling, that generalizes the provider-registry pattern already proven for images (`ImageProviderFactory`) to text/chat generation — deliberately superseding ADR-0001 ("Single AI Provider, No Gateway") for that specific case, per `.claude/decisions/ADR-0021-ai-core-foundation.md`. Reached implementation only after this engagement's full process: a Phase 0 Technical Design Document, a Phase 0.5 Architecture Review that caught and required fixing six real gaps (a missing Capability/Brain two-level indirection, a `capabilities`/`Capability` naming collision, a dropped AI Playground, an unreserved rate-limit column, no routing-cache strategy, and no provider-health service), and an Architecture Freeze before any code was written. **Architecture (frozen)**: `Business Module → AiCapability → AiBrain → AiRoutingService → AiCoreProviderFactory → Provider Plugin` — a business module or future n8n workflow invokes a Capability by key only, never a Brain/Provider/Model/Prompt directly; Brain-direct invocation is `aicoreadmin`-gated, administrative/Playground-only. Schema: 11 new tables, 5 new enums, 2 migrations, zero changes to any pre-existing table. `AiRoutingService` is the real engine — Capability→Brain resolution with an in-memory cache invalidated by an `AiAuditEvent`-driven `EventEmitter` (no Redis), `{{placeholder}}` prompt templating, a corrective-retry loop (mirroring the pattern already proven in `VideoWorkflowPlannerService` and `eyan-automation-hub` Workflow 3), a configured per-Brain fallback (a deliberate, explicit revisit of ADR-0020 Decision 7's "no automatic failover," closed by `AiUsageLog`'s per-call provider/model attribution), and HTTP-status-based failure classification mirroring Workflow 3's own Classify Ollama Result node. Four provider plugins registered (`AiCoreProviderFactory`, mirrors `McpConnectorFactory`'s no-default registry shape): `OllamaAiProvider` (a direct, unchanged-wire-contract generalization of the existing `OllamaProvider`) plus new, thin, REST-only `OpenAiAiProvider`/`AnthropicAiProvider`/`GeminiAiProvider` (no new SDK dependency) — structurally complete but not live-verified this session (no API keys / no running local Ollama available). AI Playground is real and isolated from production by construction: every execution is domain-tagged `"ai-core-playground"` on the same `AiUsageLog` table (no duplicate logging system), never persists an override, never touches the cache. Full CRUD API (`/api/v1/ai-core/*`) and a full frontend module (`features/ai-core/`, 10 real pages under a new "AI Core" sidebar group) ship in this phase. RBAC: new `aicore`/`aicoreadmin` permissions (seeded, granted to Owner), plus a bundled fix for a pre-existing gap (`finance`/`automation`/`automationcredentials` were missing from the frontend Roles-UI permission registry despite being backend-enforced). 799/799 backend tests passing (26 new — `AiRoutingService` fully unit-tested via dependency injection covering retry/fallback/definitive-failure/confidence/caching/cache-invalidation/admin-isolation, plus `AiAuditService`, a repository, and a controller), typecheck clean both sides, frontend build/lint clean. **Phase 1 changes zero existing call sites** — `ChatService`, `ContentService`, `VideoWorkflowPlannerService`, `VideoAssetService`, and `eyan-automation-hub` Workflow 3 all continue exactly as built; `eyan-automation-hub` itself was not touched at all this phase. Phase 2 (migrating those call sites to Capabilities) and Phase 3 (re-pointing Workflow 3 at AI Core over HTTP) are named, sequenced, and each require their own separate, explicit approval before starting. Full detail: `.claude/decisions/ADR-0021-ai-core-foundation.md` and `tasks/completed/sprint-ai-core-phase1.md`.

---

**CRM Foundation — Sprint 2 (Automation Integration Contract, EYAN side only) — COMPLETE.** Built the EYAN half of the cross-system contract with `eyan-automation-hub` (n8n) — `eyan-automation-hub` itself was explicitly excluded from this sprint (a repo-boundary conflict was surfaced and the user confirmed EYAN-side-only before implementation began) — so nothing in that repo changed; the contract was verified with curl standing in for n8n. Started by writing `.claude/decisions/ADR-0019-automation-integration-contract.md` (webhook auth, service auth, retry/timeout/idempotency/versioning policy, and rejected alternatives), per the sprint brief's explicit "do not begin implementation until ADR-0019 is complete." Backend, no schema changes (every field Sprint 2 writes to already existed from Sprint 1's schema-ahead-of-use design): `authenticateService` middleware (static bearer token `AUTOMATION_SERVICE_API_KEY`, `crypto.timingSafeEqual` comparison, fails closed with 500 if unconfigured rather than authenticating an empty value), `AutomationWebhookService` (HMAC-SHA256-signed, fire-and-forget outbound dispatch to n8n's future Lead Intake webhook on every new lead — never awaited, never blocks the Lead Form response), and `CrmAutomationIngestService` (the n8n write-back surface: `GET /crm/service/leads?email=` dedupe check, `PATCH /crm/service/leads/:id/validation` for Workflow 2's VALIDATED/DISQUALIFIED result, `PATCH /crm/service/leads/:id/qualification` — this sprint's "dummy qualification response" deliverable, the real Workflow 3/4 write-back contract exercised with a stub payload instead of a real AI call). Every service mutation is idempotent on `{ workflowName, workflowExecutionId }`, checked against `WorkflowExecutionLog` before applying (a query-then-write app-level check, not a DB unique constraint — ADR-0019 Decision 5). `CrmAutomationIngestService` imports `CrmLeadService.ALLOWED_TRANSITIONS` (now exported) rather than duplicating the lifecycle map — one authority regardless of which side triggers a transition. `AUTOMATION_SERVICE_API_KEY`/`AUTOMATION_WEBHOOK_SIGNING_SECRET` follow `AUTOMATION_ENCRYPTION_KEY`'s established precedent (optional, empty default, fail-closed on first use) rather than the TDD's originally-stated `requireEnv()` — recorded as a deliberate implementation adjustment in ADR-0019, since requiring them at boot would have broken every existing dev/CI/test environment. No frontend work — not in this sprint's scope. 773/773 backend tests passing (45 new: signature utility, `authenticateService`, `WorkflowExecutionLogRepository`, `CrmAiAnalysisRepository`, `AutomationWebhookService`, `CrmAutomationIngestService` — including idempotent-replay and illegal-transition coverage for both write-back endpoints, `CrmAutomationController`, and the automation validators), typecheck clean. **Live-validated** against the real EYAN Studio development database, using a throwaway backend instance on a spare port (PM2 production `:3001` untouched) and a temporary, since-deleted Owner-role verification user: confirmed the auth gate (missing header, wrong token, unconfigured key, correct token), the dedupe lookup (miss then hit), a full validation → qualification chain with the resulting `Lead`/`LeadActivity`/`LeadAiAnalysis`/`WorkflowExecutionLog` rows inspected via the user-facing detail endpoint, idempotent replay of both write-back endpoints (confirmed a replay with different — deliberately wrong — payload values did NOT overwrite the original result), an illegal-transition rejection (400), and a malformed-payload rejection (400). One pre-existing-clutter issue discovered and cleaned up mid-session, unrelated to this sprint's own code: six orphaned `tsx watch` processes from prior sessions (dated Jul30) were still running and one had auto-restarted on port 3099 in response to this sprint's file edits, intercepting the first round of smoke-test requests with a stale environment — all six were identified, confirmed non-production (the real systemd-managed backend is a separate `dist/index.js` process), and terminated before re-running the smoke test cleanly. Full detail: `.claude/decisions/ADR-0019-automation-integration-contract.md` and `tasks/completed/sprint-2-automation-integration-contract.md`.

---

**CRM Foundation — Sprint 1 (Leads: CRUD, lifecycle, timeline, notes) — COMPLETE.** A new module added outside the original Version 1.0 scope (`docs/product/02_ROADMAP.md` lists CRM as out of scope for V1.0 — this is a parallel initiative, not a resumption of that scope), positioned as the first pillar of a future Sales Workspace rather than a point solution, per an approved Technical Design Document (`/home/eyancantimbuhan/.claude/plans/project-ai-sales-clever-dijkstra.md`) that itself went through a dedicated Phase 0.5 architecture review before implementation began. Four new, purely additive tables (`Lead`, `LeadActivity`, `LeadAiAnalysis`, `WorkflowExecutionLog`), gated behind a new `crm` permission (seeded, auto-granted to `Owner`). Shared workspace, same posture as Finance — no per-lead ownership, `assignedToId` records the responsible rep but is never used for access control. Backend: full `/api/v1/crm/leads` surface (public rate-limited intake + authenticated list/detail/update/status/assign/notes), a server-enforced lifecycle transition map (`CrmLeadService.ALLOWED_TRANSITIONS` — `NEW → VALIDATED → AI_ANALYZED → QUALIFIED → CONTACTED → NEGOTIATION → CONVERTED`, `LOST` reachable from `VALIDATED` onward, `NEW → DISQUALIFIED` as the only path out of `NEW`), status/assignment writes that await their paired `LeadActivity` row rather than firing-and-forgetting it (unlike Finance's audit log — this timeline is user-visible, not supplementary). `LeadAiAnalysis`/`WorkflowExecutionLog` exist in the schema now but are populated by nothing this sprint — deliberately schema-only, so Sprint 2/3's n8n integration doesn't need a second migration; `WorkflowExecutionLog.domain` (default `"crm"`) is platform-wide from day one, not CRM-owned, for the same forward-compatibility reason. Frontend: `features/crm/` — Dashboard (stat cards + pipeline-by-status chart), Leads table, Lead Detail (AI Analysis empty-state card, merged Activity/Automation timeline with inline note entry, Status/Assign/Edit dialogs, all reset-on-open via `react-hook-form.reset()` rather than raw `useState` in an effect, to avoid tripping `react-hooks/set-state-in-effect` — see `McpServerDialog`'s pre-existing identical pattern), new "Sales" sidebar group. 728/728 backend tests passing (37 new), typecheck/build clean (backend has no lint script). Frontend typecheck/build/lint clean (0 errors in new code; 2 pre-existing-pattern `react-hooks/incompatible-library` informational warnings, identical in kind to Finance's/Roles'/Users' own table components), full suite 410/414 passing, 4 failing — the same pre-existing, already-documented baseline (`search-provider.test.tsx` ×2, `user-auth-form.test.tsx` ×2), reconfirmed identical against the pre-CRM baseline via `git stash` before this sprint's sidebar change. **Live-validated** against the real EYAN Studio development database, using a throwaway backend instance on a spare port (PM2 production `:3001` untouched) and a temporary, since-deleted Owner-role verification user: full create (public endpoint) → list → detail → status transition (valid + invalid-transition-rejected) → note cycle via curl, each step's response inspected. One deliberate scope adjustment from the TDD's original Sprint 1 boundary, not a silent architecture change: the TDD's own sprint plan had put the n8n-facing service-authenticated routes in Sprint 1 alongside user-facing CRUD; this sprint's explicit brief excluded Automation/Webhooks, so `authenticateService`/`AUTOMATION_SERVICE_API_KEY`/`AUTOMATION_WEBHOOK_SIGNING_SECRET` and the `/crm/leads/service/*` routes are deferred to Sprint 2 — recorded in `.claude/decisions/ADR-0018-crm-foundation.md` Decision 6. Full detail: `.claude/decisions/ADR-0018-crm-foundation.md`.

---

**Authentication & Public Experience Fix — Phase 2C — COMPLETE.** Investigated and fixed a real bug: `AuthProvider` wrapped the entire router in `main.tsx`, so every route — public and protected — waited behind a global "Restoring session..." gate, and a stale-but-present 7-day session cookie could hard-redirect a first-time visitor to `/sign-in` before the public landing page ever rendered. Routing structure itself was already correct and is untouched. Fix: `AuthProvider` is now scoped to `/app/_authenticated` only (moved into `components/layout/authenticated-layout.tsx`); public routes render immediately with zero auth dependency. A guard skips redundant restoration when `auth.user` is already set (post-login, including the Watch Presentation demo flow), so it isn't affected by AuthProvider now mounting fresh on every entry into `/app`. Live browser verification of the "expired session" scenario surfaced two real bugs in `services/api.ts`'s token-refresh interceptor: a circular deadlock (a doubly-invalid access+refresh token hung forever on "Restoring session..." instead of ever redirecting, because the refresh call's own 401 recursively re-entered the same retry-via-refresh logic and awaited its own in-flight promise) and nested `redirect` search params from concurrent 401s each independently navigating. Both fixed; the hard `window.location.href` redirect was also replaced with SPA `router.navigate()` via a new minimal `lib/router-instance.ts` holder (avoids a circular import from `main.tsx`). All 6 required scenarios verified in a real Chromium session (fresh browser, valid session, expired session — both landing and `/app`, and the full Watch Presentation flow through to a completed tour) — zero console errors, zero regressions (404/408 tests passing, same 4 pre-existing unrelated failures; lint/typecheck clean). RBAC, the Presentation Engine, and the demo-account flow are unchanged. Full detail: `.claude/decisions/ADR-0017-auth-provider-scoping.md`.

---

**Presentation Engine — Phase 2B (AI Content Studio as a cinematic product story) — COMPLETE.** The Presentation Engine Core remained frozen — only the AI Content Studio Tour Pack's content changed, per `.claude/decisions/ADR-0016-presentation-engine-phase-2b.md`. The tour was rewritten from 6 shallow, dashboard-level scenes into an 18-scene, single-story walkthrough of the real project workspace (`frontend/src/features/presentation-engine/scenes/content-studio.scenes.ts`, curated by `tour-packs/ai-content-studio-tour.ts`): Overview → Content (templates/generation/history) → Images → Brand Kit → Video → Asset Library → Review → Publishing → Analytics → Summary, narrated for business value rather than UI description. A new, fully-seeded demo `ContentProject` ("Nimbus Coffee Co. — Spring Launch," `backend/prisma/seed-demo-content.ts`, called from `seed.ts`) gives every scene real generated content, real viewable images (encoded from scratch as solid-color PNGs via Node's built-in `zlib`, no image-processing dependency, written to the same `backend/storage/images/` path the real storage provider serves), a real Brand Kit, video production artifacts, and a mixed review/publishing/analytics state — seeded directly via Prisma, bypassing every real AI provider (idempotent; safe to re-run). Two scenes perform real interactions through the engine's previously-unused `custom` action escape hatch: selecting a real template card, and opening a real asset's detail view. A real bug was found and fixed during verification: closing that asset sheet was first implemented as a simulated Escape keypress, which collided with the frozen engine's own global "Escape exits the tour" shortcut and silently killed the whole presentation with no console error — fixed by clicking the Sheet's real close button instead (`data-presentation-target='ui.sheet-close'`, added to the shared `components/ui/sheet.tsx` primitive). 13 new `data-presentation-target` attributes were added across real Content Studio workspace components. The demo account's `Viewer` role gained the `presentation-engine` permission so it can own the seeded project and launch the tour directly. Verified end-to-end in a live Chromium session — full 18-scene tour, zero console errors. Full detail: `.claude/decisions/ADR-0016-presentation-engine-phase-2b.md`.

---

**Presentation Engine — Phase 2A (Presentation Library + AI Content Studio) — COMPLETE.** Built on Phase 1's frozen Core (`.claude/decisions/ADR-0014-presentation-engine-foundation.md`) with zero engine changes — only new scene data, two new Tour Packs, and page-level UI, per `.claude/decisions/ADR-0015-presentation-engine-phase-2a.md`. The public homepage (`frontend/src/features/portfolio/PortfolioLanding.tsx`) now has a normal SaaS layout — Login, "Watch Presentation" (launches a new curated `recruiter-tour` Tour Pack; never auto-starts), Case Study (rewritten to accurately describe the new engine), and a GitHub link — replacing the old real-pipeline "Start Interactive Demo" CTA (the underlying `features/portfolio` tour system is untouched, just no longer wired to the homepage). The authenticated `/app/presentation-engine` route is now a real "Presentation Library": four playable Tour Packs (Platform Overview, the new **AI Content Studio** — 6 scenes across the project list, new-project flow, content pipeline, Production Dashboard, and Prompt Library, deliberately scoped to routes with no dynamic `$projectId` dependency — plus curated **Recruiter Tour** and **Customer Tour**) and two static "Coming Soon" placeholders (AI Chat, Finance). Six new `data-presentation-target` attributes were added to Content Studio components, following Phase 1's convention exactly. Live browser verification (full flow: anonymous homepage → Watch Presentation → authenticated library → AI Content Studio tour → exit, zero console errors after one fix) caught a real permission gap: the public demo account had no roles at all and 403'd on the Dashboard's models endpoint mid-presentation — fixed with a new, explicitly least-privilege `Viewer` role (`dashboard`/`chat`/`models`/`conversations` only) rather than granting it `Owner` (the demo credential is effectively public) or leaving the presentation broken. Full detail: `.claude/decisions/ADR-0015-presentation-engine-phase-2a.md`.

---

**Presentation Engine — Phase 1 (Core) — COMPLETE.** A new platform capability that presents the real, existing application — narration, subtitles, spotlight highlights, camera framing, and cross-route navigation — rather than duplicating any page or component, per an approved Technical Design Document (`.claude/decisions/ADR-0014-presentation-engine-foundation.md`). Scenes are plain data resolved by a Scene Manager → Timeline Engine pipeline that only ever does what a real click/scroll/navigate could also do; a new `data-presentation-target` attribute (piloted on Dashboard and AI Chat) is the component-targeting convention, deliberately separate from `data-slot`/`data-testid`. All motion is native `scrollIntoView`/CSS `@keyframes` behind a swappable `AnimationDriver` seam — no new animation dependency, consistent with this codebase having none before this phase. Playback state lives in a new root-level, non-persisted `presentation-store.ts` (Zustand, modeled on the existing `features/portfolio` tour's `tour-store.ts`, which is untouched — it solves a different problem and is not migrated). A new `/app/presentation-engine` launcher route is gated by a new `presentation-engine` permission, which — non-obviously — had to be seeded on **both** the frontend Roles-UI registry and the backend `Permission` table (`backend/prisma/seed.ts`) to actually grant access; this was caught during live browser verification, not assumed. That same verification surfaced and fixed a real pacing bug: a scene with narration text but no audio asset (Phase 1's only narration provider) resolved near-instantly, racing through all five scenes of the first Tour Pack in under two seconds, before the Scene Manager's floor-duration top-up was corrected to run unconditionally rather than being skipped whenever any timed step had merely run. The one shipped Tour Pack, "Platform Overview" (Dashboard + AI Chat, 5 scenes), was confirmed end-to-end in a real Chromium session: sign-in → RBAC-gated launcher → Start Tour → real cross-route navigation with spotlight/callout/narration/progress/speed/pause all working → real AI Chat page reached → Exit fully tearing down the overlay with the real app left intact, zero console errors. TTS narration providers (OpenAI/ElevenLabs/Google/Azure), subtitle localization, and full platform scene coverage are explicitly Phase 2+ — out of scope here. Full detail: `.claude/decisions/ADR-0014-presentation-engine-foundation.md`.

---

**Sprint A — Finance Management Foundation (Dashboard + Expenses) — COMPLETE.** A new Finance Management module — a household expense/budget tracker for two Admin users, per an explicit architecture review and approved Technical Design Document (this repo's "Finance Management" module is a net-new addition to the product blueprint, not a previously-roadmapped feature; `docs/product/02_ROADMAP.md` had explicitly listed "Accounting"/"Billing" as out of scope for V1.0). Four new, purely additive tables (`Expense`, `RecurringExpenseTemplate`, `Budget`, `FinanceAuditEvent`), gated behind a new `finance` RBAC permission (seeded, auto-granted to `Owner`; manually granted to `Admin` this sprint, mirroring the one-time Roles-UI step a real Owner would take). Money (`Expense.amount`, `Budget.monthlyLimit`) is this codebase's first `Prisma.Decimal` usage — converted to a fixed-point string only at the DTO/mapper boundary, aggregated in PostgreSQL, never summed via JS floats. Recurring expenses (an optional "Repeat monthly" toggle on the Expense form) are generated **lazily, on read** — `FinanceGenerationService.ensureCurrentPeriodGenerated()` — rather than via a scheduler, since no cron/queue/background-worker infrastructure exists anywhere in this codebase; it backfills every missed period (not just the current one), clamps `dayOfMonth` to short months, and is DB-idempotent (`@@unique([recurringTemplateId, period])`). A new "Finance Management" sidebar group (Dashboard, Expenses) is this codebase's first real use of the `NavCollapsible` parent-with-children shape — `nav-group.tsx` already fully supported it. The Dashboard's category-breakdown chart is this codebase's first Pie chart (alongside the first Line chart, for the spending trend) — building it surfaced and fixed a real Recharts bug (a single 100%-value slice fails to render any arc at all; fixed with a plain-SVG fallback for that one case). Bills, Savings, Reports, receipt upload, and recurring-template management UI are explicitly out of scope this sprint (later sprints per the roadmap). Full detail: `tasks/completed/sprint-A-finance-management.md` and `.claude/decisions/ADR-0013-finance-management-foundation.md`.

---

**Sprint 7.2.4 — AI Video Editing Pipeline, AI Transcription & Subtitle Generation — COMPLETE.** The fourth milestone of Sprint 7.2, built on Sprint 7.2.3's Execution Engine. `subtitles` — planner-supported since Sprint 7.2.2, rejected as not-yet-executable since Sprint 7.2.3 — is now a real, executable operation. A new `WhisperTranscriptionProvider` spawns a dedicated Python subprocess (`faster-whisper`, CPU, `int8`, model size configurable via `WHISPER_MODEL`, default `small`) to transcribe an already-extracted audio file into timestamped segments and build a standard `.srt`; it never touches a video file directly. `FFmpegVideoProvider` gained `extractAudio()` and `burnSubtitles()`. `VideoExecutionEngineService` orchestrates the full sequence for a `subtitles` step only — extract audio → transcribe → write `.srt` → burn subtitles — while every other step is unchanged; two new guards (no audio track, no speech detected) surface as the same clean `VideoExecutionFailedError` any other step failure already produces. `VideoAsset` gained one additive, nullable `subtitlePath` column, populated only when the executed plan included a `subtitles` step; no new API endpoint was needed — the existing execute response simply carries it. The `VideoWorkflowExecutor` panel gained a one-line "Subtitles: Generated — preview .srt" (or "None") display, kept deliberately minimal — no subtitle editor, timeline, or styling controls. The planner itself is unchanged. No OpenCV/MediaPipe, face detection, auto zoom, background music, background workers, queues, or progress polling exist yet — later milestones, explicitly out of scope here. Full detail: `tasks/completed/sprint-7-2-4-whisper-transcription.md`.

---

**Sprint 7.2.3 — AI Video Editing Pipeline, Execution Engine + Core FFmpeg Operations — COMPLETE.** The third milestone of Sprint 7.2, built on Sprint 7.2.2's Workflow Planner. A user can now actually execute an approved `VideoWorkflowPlan` — `VideoExecutionEngineService` loads it, re-validates it (defense-in-depth against a `Json` column with no runtime schema guarantee), rejects wholesale if it contains any operation not yet executable, then runs each step sequentially through a new `FFmpegVideoProvider` (`trim`, `remove_silence`, `normalize_audio`, `resize`, `brightness` — the five operations this milestone actually implements; `subtitles`/`blur_faces`/`auto_zoom`/`background_music`/`shorts` remain plannable but are rejected at execution time, never partially run). No AI decisions happen during execution — the stored plan runs exactly as written. The uploaded source is always *copied*, never moved, into a temp working directory that's removed on both success and failure; the result is `ffprobe`d for real metadata and persisted as a new `VideoAssetKind.EDITED_VIDEO` (a fourth additive enum value), grouped with its source via the same `videoGroupId` (no new FK needed) — meaning Asset Library, Review, and Analytics all pick it up for free. `POST /api/v1/video-edit/execute` and a new "Execute a Workflow" panel (workflow picker, Execute button, result preview) round out the milestone; a small necessary addition, `GET /api/v1/video-edit/planner?projectId=`, was also added since the frontend now needs to list previously generated plans, which didn't exist as a requirement until this milestone. No Whisper, OpenCV, background workers, queues, or progress polling exist yet — later milestones, explicitly out of scope here. Full detail: `tasks/completed/sprint-7-2-3-execution-engine.md`.

---

**Sprint 7.2.2 — AI Video Editing Pipeline, Workflow Planner — COMPLETE.** The second milestone of Sprint 7.2, built on top of Sprint 7.2.1's Source Ingestion. A user can now describe how they want to edit an uploaded video in plain English (e.g. "Remove silence, make it vertical, add subtitles and normalize the audio.") and the AI converts that into a validated, structured editing plan — **the plan is only ever generated and stored, never executed.** `VideoWorkflowPlannerService` calls the existing `ChatService`/Ollama provider directly (no new AI abstraction, same precedent `VideoAssetService`'s text kinds already set), grounding the planner prompt in the source video's real `ffprobe` metadata from Sprint 7.2.1. The model's raw JSON output is never trusted: it's parsed and validated against a strict `z.discriminatedUnion` Zod schema of a fixed ten-operation catalog (`trim`, `remove_silence`, `normalize_audio`, `resize`, `shorts`, `subtitles`, `blur_faces`, `auto_zoom`, `brightness`, `background_music`), each with bounded, `.strict()` parameters — unknown operations, missing/invalid/out-of-bounds/extra parameters, and malformed JSON are all rejected by construction. One corrective retry is attempted on failure (feeding the model its own bad output plus the exact validation error); if that also fails, a clean 422 is returned and nothing is persisted. A new, purely additive `VideoWorkflowPlan` model stores only the prompt, the validated workflow JSON, and a planning timestamp — deliberately no execution/job state, which is the Execution Engine milestone's (7.2.3+) own concern. `POST /api/v1/video-edit/planner` and a new "Plan an Edit" panel inside the existing Video tab (source picker, prompt textarea, Generate Plan button, ordered step preview) round out the milestone. No FFmpeg execution, Whisper, OpenCV, background workers, or job/queue infrastructure exists yet — explicitly out of scope here. Full detail: `tasks/completed/sprint-7-2-2-workflow-planner.md`.

---

**Sprint 7.2.1 — AI Video Editing Pipeline, Source Ingestion — COMPLETE.** The first milestone of Sprint 7.2 (AI Video Editing Pipeline), per the architecture proposal reviewed and approved ahead of implementation — **this redirects Sprint 7.2 away from the Canva MCP Provider previously assumed to be next** (see Sprint 7.1's closing note below); Canva/GitHub/Docker/etc. MCP connectors remain valid, un-started future work, just not this track. Users can now upload a real video file into the existing AI Video Studio; it becomes an ordinary `VideoAsset` (`VideoAssetKind.UPLOADED_SOURCE`, two new additive enum values reserved: this one plus `EDITED_VIDEO` for a later milestone) — searchable/reviewable/analytics-tracked for free, since `AssetType.VIDEO` already aggregates every `VideoAssetKind`. This is the first real video *file* (not just AI-generated production artifacts) and the first real file upload anywhere in this codebase: `POST /api/v1/video-edit/sources` streams a multipart upload straight to disk (`multer`, disk storage, never memory storage — the backend runs under PM2 with a 500MB `max_memory_restart` ceiling, `ecosystem.config.cjs`), `ffprobe` (`child_process.spawn`, argv array, never a shell string) both extracts real metadata (duration/resolution/codec/container) and serves as the authoritative "is this actually a video" check, and `StorageProvider.save()` gained an optional `sourcePath` input so the file moves (`rename()`, `EXDEV` fallback) into the storage root without ever being buffered into Node memory. No planner, execution engine, FFmpeg editing, Whisper, or background job infrastructure exists yet — those are later Sprint 7.2 milestones, explicitly out of scope here. Full detail: `tasks/completed/sprint-7-2-1-source-ingestion.md`.

---

**Sprint 7.1 — MCP Foundation — COMPLETE.** The first phase of Sprint 7 (AI Automation Platform, per the Sprint 7 architecture review that closed out Sprint 6) is done: the reusable integration layer future automation providers (Canva, CapCut, GitHub, Docker, Filesystem, Google Drive, Slack, Discord, Notion, PostgreSQL, Redis, n8n) will register into, with **no real provider implemented yet** — only the foundation, proven end-to-end with one deterministic `FakeMcpConnector`. Three new, purely additive tables (`AutomationConnection`, `McpServerConfig`, `AutomationAuditEvent`), zero changes to any of the 29 pre-existing models. A new `McpConnectorFactory` registry mirrors `ImageProviderFactory`'s proven shape (one deliberate difference: no env-var default, mirroring `PlatformProviderFactory` instead, since many MCP servers run simultaneously). Credentials are encrypted at rest (AES-256-GCM) by a single service (`CredentialManagerService`) that nothing else in the codebase bypasses — no API response, and no `McpConnector` implementation, ever sees a raw credential. Full REST API (`/api/v1/automation/{connections,mcp-servers,audit-logs}`) and a complete management UI (a new "Automation" sidebar section: Providers, Connections, MCP Servers, Health, Audit Logs), gated by two new permissions (`automation`, the stricter `automationcredentials`) plus reuse of a pre-existing, previously-unwired `auditlogs` permission. Full detail: `tasks/completed/sprint-7-1-mcp-foundation.md` and `.claude/decisions/ADR-0012-mcp-foundation.md`.

Sprint 7.1 was implemented across 6 reviewed milestones (Database → MCP Core → Repositories → Services → API → Frontend), each stopped for explicit approval before the next began, per the user's working agreement. **A real Canva Provider was the natural next step assumed at the time this sprint closed — that has since been superseded**: Sprint 7.2 was explicitly redirected to the AI Video Editing Pipeline instead (see Sprint 7.2.1 above), following its own dedicated architecture review. A Canva/GitHub/Docker/etc. MCP connector remains valid, un-started future work.

---

**Sprint 6 — Enterprise Creative Production Suite — COMPLETE.** **All six phases (6.1 Brand Kits, 6.2 AI Video Studio, 6.3 Creative Review Workspace, 6.4 Publishing Pipeline, 6.5 Analytics Foundation, 6.6 Production Dashboard) are complete and live-validated** (see `tasks/completed/sprint-6-1-brand-kits.md` through `tasks/completed/sprint-6-6-production-dashboard.md`). Every project can now own one or more Brand Kits (logos, colors, fonts, tone of voice, terminology, restricted words, CTA style, and free-text guidelines) and generate video-production artifacts of eight kinds (Script, Scene Breakdown, Shot List, Voice-over Script, Captions, Subtitles, Storyboard, Thumbnail), grouped into "one video" via a plain `videoGroupId` column with no second project-level entity. Content, image, and video generation can all optionally reference a Brand Kit — folded into the prompt, never enforced. Both Brand Kits and Video Assets are first-class Asset Library assets (`AssetType.BRAND_KIT`/`AssetType.VIDEO`) via the exact ADR-0008 recipe: one new enum value, one new mapper case, zero changes to `AssetReview`/`AssetVersion`. Every asset can now also be commented on and annotated (image regions, video timestamps), assigned to a reviewer (informational only — see below), and has a full review activity timeline; the review lifecycle gained a `REVISION_REQUESTED` status. An approved asset can now be scheduled and published to an external platform via a new, independent `PublishingRecord`/`PublishingStatus` lifecycle (`DRAFT/SCHEDULED/PUBLISHING/PUBLISHED/FAILED/ARCHIVED`) — gated on QA approval but otherwise decoupled from `ReviewStatus`, using a pluggable, fake-only-for-now provider registry and feeding the same review timeline. A read-only `AnalyticsService` aggregates existing review/publishing/generation data into project and platform-wide summaries and a unified activity feed, backed by exactly one new table (`AnalyticsEvent`, covering the sole gap: generation itself was never logged anywhere) written fire-and-forget so an analytics failure can never interrupt a real user workflow. Finally, a **Production Dashboard** — a dedicated Content Studio page (`/content-studio/dashboard`, its own sidebar entry, deliberately separate from the platform's existing home dashboard) plus a matching per-project "Analytics" tab — visualizes all of the above via `recharts` charts, status-count lists, performance stat tiles, and the unified activity feed. Followed **Sprint 5 — Enterprise Asset Library & QA Workflow** (`tasks/completed/sprint-5-asset-library-qa.md`).

**All six of Sprint 6.1's through 6.6's migrations are validated against the real EYAN Studio development database (2026-07-27).** Running locally (no longer sandboxed), with a reachable PostgreSQL instance: Sprint 6.1's `20260726115523_add_brand_kits` migration (hand-authored, from the earlier sandboxed session) applied via `prisma migrate deploy` with no modification needed; Sprint 6.2's `20260727090212_add_video_assets`, Sprint 6.3's `20260727101319_add_creative_review_workspace`, Sprint 6.4's `20260727131709_add_publishing_pipeline`, and Sprint 6.5's `20260727142118_add_analytics_foundation` migrations were all generated directly via `prisma migrate dev` against this same database (no hand-authoring needed at all). Sprint 6.6 introduced no migration — its one backend addition (`GET /analytics/activity`) queries existing tables only. `prisma migrate diff`/`migrate status` confirmed zero drift after each of the five migrations. Full live end-to-end passes for all six phases (CRUD/generation, Asset Library, Asset Review/Version, comments/annotations/assignment/timeline, scheduling/publishing/retry/archive, analytics summaries/activity feed, and — for 6.6 specifically — a real Playwright browser session against the live dev server confirming the dashboard/tab actually render correctly, not just API-level checks; regression on every pre-existing flow throughout) also completed successfully — see Status below and each sprint log for detail. Application code is still not deployed to production — no `deploy.sh` run since Sprint 3.5 — only this local development validation has occurred. The production nginx `/uploads/` gap flagged in Sprint 4.3 is also still open.

---

## Status

**CRM Foundation — Sprint 2 (2026-07-31):** Built exactly the scope this sprint's brief defined — `authenticateService`, `AUTOMATION_SERVICE_API_KEY`, `AUTOMATION_WEBHOOK_SIGNING_SECRET`, `/crm/service/*` endpoints, signed webhook dispatch, idempotency — and explicitly nothing beyond it (no real AI, no Ollama call, no notifications, no Sprint 3, no `eyan-automation-hub` repo changes — confirmed with the user up front after a repo-boundary conflict surfaced against the original "do not touch eyan-automation-hub" constraint). No migration — every field this sprint writes to (`WorkflowExecutionLog.n8nExecutionId`/`workflowName`/`status`, `LeadAiAnalysis`'s full field set) already existed from Sprint 1. New minimal surface: `middleware/service-auth.middleware.ts`, `services/automation-webhook.service.ts`, `services/crm-automation-ingest.service.ts`, `repositories/workflow-execution-log.repository.ts`, `repositories/crm-ai-analysis.repository.ts`, `dto/crm-automation.dto.ts`, `validators/crm-automation.validator.ts`, `controllers/crm-automation.controller.ts`, `routes/v1/crm-service.routes.ts`, `utils/automation-signature.ts` — all following the same layered/DTO→Validator→Repository→Service→Controller→Route pattern as every prior module. 773/773 backend tests passing (45 new), typecheck clean (backend has no lint script). No frontend changes this sprint. **Live-validated** against the real EYAN Studio development database using a throwaway backend instance on a spare port and a temporary, since-deleted Owner-role verification user — full auth-gate, dedupe, validation→qualification chain, idempotent-replay, illegal-transition, and malformed-payload checks via curl, each response inspected; all smoke-test data cleaned up afterward. Full detail in `.claude/decisions/ADR-0019-automation-integration-contract.md` and the sprint log.

**CRM Foundation — Sprint 1 (2026-07-31):** Built exactly the scope this sprint's brief defined — CRM data model, lifecycle, and CRUD surface — and explicitly nothing from Sprint 2+ (no n8n, no AI, no webhooks, no notifications). Backend: one migration (`20260731143055_crm_foundation_sprint1`, zero drift, `prisma migrate dev`), one new permission (`crm`, seeded), full layered implementation under `/api/v1/crm/leads` following every existing convention (`express-validator`, `ApiResponse`/`ApiError`, `requirePermission`, the Finance/Automation 8-artifact DTO→Validator→Repository→Service→Controller→Route→Permission→Test order). New minimal dependency: `express-rate-limit`, scoped only to the public lead-intake route. Frontend: `features/crm/` module, TanStack Query hooks, zod-validated Edit/Status/Assign dialogs (react-hook-form throughout, including the two Select-only dialogs — matching `McpServerDialog`'s established reset-on-open idiom rather than raw `useState`+`useEffect`), a TanStack Table Leads view reusing the shared `data-table/*` components, and a `recharts` pipeline-by-status Pie chart reusing Finance's single-100%-slice donut workaround verbatim. 728/728 backend tests passing (37 new: repository, service — including full lifecycle-transition coverage, controller, validator, permission), typecheck clean (backend has no lint script); frontend typecheck/build/lint clean, full suite 410/414 passing, 4 failing — the same pre-existing, already-documented baseline (`search-provider.test.tsx` ×2, `user-auth-form.test.tsx` ×2), reconfirmed against the pre-CRM baseline via `git stash` before this sprint's sidebar change, where they failed identically. **Live-validated** against the real EYAN Studio development database, using a throwaway backend instance on a spare port (PM2 production `:3001` untouched) and a temporary, since-deleted Owner-role verification user: public lead creation (valid + validation-rejected), authenticated list/detail, a valid status transition (NEW→VALIDATED, `LeadActivity` row confirmed), an invalid transition correctly rejected (VALIDATED→QUALIFIED, HTTP 400), and note creation producing a correctly-ordered timeline — all via direct curl against the live server, each response inspected. Full detail in `.claude/decisions/ADR-0018-crm-foundation.md`.

**Sprint A — Finance Management Foundation (2026-07-30):** Built Phase 1 MVP exactly as designed in the approved Technical Design Document — Expense CRUD, monthly Budget (with automatic prior-month carry-forward), Finance Dashboard (4 stat cards, spending-trend line chart, category-breakdown pie chart, recent expenses), and lazy recurring-expense generation. Backend: one migration (`20260729194102_add_finance_core`, zero drift, `prisma migrate dev`), one new permission (`finance`, seeded), full layered implementation under `/api/v1/finance/{dashboard,expenses,budget}` following every existing convention (`express-validator`, `ApiResponse`/`ApiError`, `requirePermission`, fire-and-forget `FinanceAuditEvent` mirroring `AutomationAuditService`). Frontend: `features/finance/` module, TanStack Query hooks, a zod-validated Expense form (only amount + category required, for sub-30-second entry), a TanStack Table Expenses view reusing the shared `data-table/*` components, and this codebase's first Line/Pie charts. 691/691 backend tests passing (23 new), typecheck clean (backend has no lint script); frontend typecheck/build/lint clean (one pre-existing `react-hooks/incompatible-library` warning, confirmed identical on the pre-existing `users-table.tsx`), full suite 366/370 passing, 4 failing — the same pre-existing, already-documented baseline (`search-provider.test.tsx` ×2, `user-auth-form.test.tsx` ×2), reconfirmed against the pre-Finance baseline via `git stash` before this sprint's sidebar change, where they failed identically. **Live-validated** against the real EYAN Studio development database, using a throwaway backend/frontend instance on spare ports (PM2 production `:3001` and the persistent dev frontend `:5180` both untouched) and a temporary, since-deleted verification user: full sign-in → sidebar → set budget → add/edit/delete expense → Dashboard reflects it cycle, each step screenshotted. Two real bugs were found and fixed during this pass: the frontend requested `pageSize=200` against a backend validator capped at 100 (surfaced as "Failed to load expenses"), and the category-breakdown Pie chart rendered nothing for a single 100%-value category (a genuine Recharts arc-drawing bug, not a logic error — see ADR-0013). Full detail in the sprint log and ADR.

**Sprint 7.2.4 — AI Video Editing Pipeline, AI Transcription & Subtitle Generation (2026-07-28):** Built Milestone 4 of the AI Video Editing Pipeline exactly as designed in the approved architecture proposal — no redesign, no scope expansion, no planner changes. Backend: `WhisperTranscriptionProvider` (`providers/whisper/whisper-transcription.provider.ts`) spawns a dedicated Python subprocess (`backend/python/transcribe.py`, a fresh venv with `faster-whisper` installed, not committed — see `backend/python/README.md`) via an argv array, never a shell string; `transcribe()` parses one JSON-on-stdout line into timestamped segments, `buildSrt()` formats standard SRT — no registry, same one-real-implementation judgment call as `FFmpegVideoProvider`. `FFmpegVideoProvider` gained `extractAudio()` (mono/16kHz PCM, Whisper's expected input) and `burnSubtitles()` (the `subtitles` ffmpeg filter, sidestepping its own path-escaping requirements entirely by running with `cwd` set to the `.srt`'s directory and referencing it by bare filename). `VideoExecutionEngineService` gained a small `isExecutableOperation()` helper (`operation === "subtitles" || ffmpegProvider.isSupported(operation)`, deliberately not folded into `FFmpegVideoProvider.SUPPORTED_OPERATIONS`, keeping that provider's own contract/tests unchanged) and a new `executeSubtitlesStep()` private method orchestrating extract-audio → transcribe → write-`.srt` → burn-subtitles for that one step only; two new guards (no audio track, no speech detected) surface as the same `VideoExecutionFailedError` any other step failure already produces. `VideoAsset.subtitlePath String?` (additive, nullable) persisted only when a `subtitles` step ran; migration `20260728093919_add_video_subtitle_path` applied via plain `prisma migrate dev` (no interactive-prompt workaround needed this time) with zero drift. `LocalDiskStorageProvider`'s extension allowlist gained `srt`. No new API endpoint — the existing `POST /api/v1/video-edit/execute` response simply carries the populated field. Frontend: `VideoWorkflowExecutor`'s result panel gained a one-line "Subtitles: Generated — preview .srt" (plain read-only link) or "Subtitles: None". 654/654 backend tests passing (16 new; one existing test's "not-yet-executable" example changed from `subtitles` to `blur_faces`, since `subtitles` is executable now), typecheck clean (backend has no lint script). Frontend typecheck/lint clean; full suite 370 tests, 366 passing, 4 failing — the same pre-existing, already-documented baseline (`search-provider.test.tsx` ×2, `user-auth-form.test.tsx` ×2), none in a file this sprint touched. **Live-validated** against the real EYAN Studio development database, the real local Ollama instance, real FFmpeg, and a real, freshly-installed `faster-whisper` (`small` model, downloaded once and cached locally, ~431MB, no `torch` dependency), using a throwaway backend instance on a spare port (the pre-existing dev backend/frontend were both left untouched throughout): asked the real planner "Add subtitles to this video, nothing else." against a synthetic speech test video (gTTS-narrated, since no local TTS tool was available) — it correctly returned a one-step `subtitles` plan; executing it for real correctly extracted audio, transcribed it accurately (`"Hello! This is a test of the video editing pipeline subtitle generation feature."`), generated a correctly-timestamped `.srt`, burned it into the video (confirmed both via `ffprobe` and by visually inspecting an extracted frame showing the correct subtitle text rendered), saved both the edited video and the `.srt` to real storage, and correctly appeared in the Asset Library grouped with its source via the shared `videoGroupId`; the original source file remained byte-identical (same size, same MD5); all temp files were removed afterward. Full detail in the sprint log.

**Sprint 7.2.3 — AI Video Editing Pipeline, Execution Engine + Core FFmpeg Operations (2026-07-28):** Built Milestone 3 of the AI Video Editing Pipeline exactly as designed in the approved architecture proposal — no redesign, no scope expansion. Backend: `VideoAssetKind.EDITED_VIDEO` (additive) plus `VideoWorkflowPlan.resultVideoAssetId`/`executedAt` (additive nullable — deliberately not a `VideoEditJob`/queue model, since execution is synchronous this milestone); `FFmpegVideoProvider` (`providers/ffmpeg/ffmpeg-video.provider.ts`) — one `run(step, inputPath, outputPath)` entry point dispatching to five ffmpeg-argv-array operations (`spawn`, never a shell string), with `remove_silence` implemented as a named MVP simplification (a `silencedetect` pass trims leading/trailing silence only, keeping A/V in sync; interior silence removal is deferred); `VideoExecutionEngineService.execute()` (load plan → re-validate stored workflow against `WorkflowSchema` → reject wholesale if any step isn't yet executable → load source asset → copy, never move, the source into a temp working dir → run steps sequentially, each output feeding the next input → `ffprobe` the result → save via the existing `StorageProvider` → persist as `EDITED_VIDEO`, reusing the source's `videoGroupId` → mark the plan executed → fire-and-forget analytics → remove the temp dir in a `finally`, covering both success and failure). `StorageProvider` gained `getAbsolutePath()` (additive interface method) so the engine can hand FFmpeg a real filesystem path. `POST /api/v1/video-edit/execute` (new) and `GET /api/v1/video-edit/planner?projectId=` (new — a small necessary addition, since the frontend now needs to list previously generated plans to satisfy "select an existing workflow", which wasn't a requirement until this milestone). `POST /video-assets/generate`'s validator now also excludes `EDITED_VIDEO` — the identical regression class Sprint 7.2.1 caught for `UPLOADED_SOURCE`, caught again here before shipping. Frontend: `VideoWorkflowExecutor` (new panel in the Video tab — workflow picker labeled with source filename/prompt/executed-flag, Execute Workflow button, result `<video>` preview); `EDITED_VIDEO` added to `VIDEO_FILE_KINDS`/`VIDEO_KIND_LABELS` so `VideoAssetList`'s existing kind-agnostic real-video-player branch renders it with zero new code. 636/636 backend tests passing (29 new), typecheck clean (backend has no lint script), one migration applied with zero drift (`prisma migrate status` confirmed — hand-authored via `prisma migrate diff --script` since this environment's non-interactive shell blocks `prisma migrate dev`'s enum-addition prompt, the same fallback Sprint 6.1 used). Frontend typecheck/lint clean; full suite 368 tests, 364 passing, 4 failing — the same pre-existing, already-documented baseline (`search-provider.test.tsx` ×2, `user-auth-form.test.tsx` ×2), none in a file this sprint touched. **Live-validated** against the real EYAN Studio development database, the real local Ollama instance, and real FFmpeg — via direct API calls and two full real-browser Playwright sessions, using a throwaway backend/frontend instance on spare ports (production/dev instances untouched throughout): confirmed the original source file byte-identical and untouched after execution; the edited output correctly resized/silence-trimmed/normalized/brightened and stored with accurate metadata; both the source and edited assets rendering correctly (grouped side-by-side via the shared `videoGroupId`) in the Video tab, Asset Library, and Review tab; a plan containing an unsupported operation (`subtitles`) correctly rejected with a clean `422` and zero side effects; temp working directories correctly removed after both success and a rejected execution. Full detail in the sprint log.

**Sprint 7.2.2 — AI Video Editing Pipeline, Workflow Planner (2026-07-28):** Built Milestone 2 of the AI Video Editing Pipeline exactly as designed in the approved architecture proposal — no redesign, no scope expansion. Backend: one new, purely additive `VideoWorkflowPlan` model (`projectId`, `videoAssetId` FK, `prompt`, `workflow` `Json`, `model`, `createdBy`, `createdAt` — no execution/status column, since planning is a synchronous request/response operation, not a job); a strict `z.discriminatedUnion` Zod schema (`validators/video-workflow-plan.validator.ts`) over the fixed ten-operation catalog (`trim`, `remove_silence`, `normalize_audio`, `resize`, `shorts`, `subtitles`, `blur_faces`, `auto_zoom`, `brightness`, `background_music`), rejecting unknown operations, missing/invalid/out-of-bounds/extra parameters, and malformed JSON by construction; `VideoWorkflowPlannerService.plan()` (ownership + `UPLOADED_SOURCE`-kind check on the source video → builds a planner prompt grounded in its real `ffprobe` metadata → calls `ChatService.chat()` directly, no new AI abstraction → parses the response, defensively stripping a markdown code fence first → validates → one corrective retry on failure, feeding the model its own bad output plus the exact error → a clean 422 and no persistence if still invalid); `POST /api/v1/video-edit/planner` (new, `authenticate`-only, Zod-inline validation in the controller mirroring `ChatController`, since this is a JSON-body endpoint rather than the multipart shape `video-source.validator.ts` handles). Frontend: `VideoWorkflowPlanner` (new panel inside the existing Video tab, between Upload and Generate — source picker derived client-side from the already-fetched video asset list, prompt textarea, Generate Plan button, ordered numbered step preview with operation labels and parameters). 607/607 backend tests passing (22 new), typecheck/build clean on both sides, one migration applied with zero drift (`prisma migrate status` confirmed). Frontend typecheck/build/lint clean; full suite 360 tests, 356 passing, 4 failing — the same pre-existing, already-documented baseline (`search-provider.test.tsx` ×2, `user-auth-form.test.tsx` ×2), none in a file this sprint touched. **Live-validated** against the real EYAN Studio development database and the real local Ollama instance (`qwen2.5-coder:7b`) via both a direct API call and a full real-browser Playwright session (registration → sign-in → project creation → Video tab → upload → select source → describe edit → Generate Plan), using a throwaway backend/frontend instance on spare ports — production/dev instances untouched throughout: the real model correctly turned the user's own example prompt into `remove_silence → shorts → subtitles → normalize_audio` on the first attempt (~99s, consistent with this CPU-only hardware's documented ~3 tok/s profile), persisted with no execution state; confirmed correct `404`/`400` handling for a missing video asset, a missing prompt, and planning against a non-`UPLOADED_SOURCE` asset (each with no AI call made). Full detail in the sprint log.

**Sprint 7.2.1 — AI Video Editing Pipeline, Source Ingestion (2026-07-28):** Built Milestone 1 of the AI Video Editing Pipeline exactly as designed in the approved architecture proposal — no redesign, no scope expansion. Backend: `VideoAssetKind.UPLOADED_SOURCE` plus three additive nullable `VideoAsset` columns (`durationMs`/`videoFormat`/`sourceFileName`); `SaveFileInput` gained an optional `sourcePath` alternative to `buffer` so `LocalDiskStorageProvider.save()` can move an already-on-disk file into the storage root (`rename()`, `EXDEV` copy+unlink fallback) instead of buffering it — necessary because the backend runs under PM2 with a 500MB `max_memory_restart` ceiling and video files are far larger than the image bytes this interface previously handled; `video-upload.middleware.ts` (multer disk storage, mimetype allowlist, size cap, `MulterError` mapped to clean `ApiError`s); `ffprobe.util.ts` (spawns `ffprobe` via argv array, never a shell string — also the authoritative "is this really a video" check, not just the mimetype filter); `VideoSourceService.ingest()` (ownership check → extension validation → ffprobe → storage → `VideoAsset` create → fire-and-forget `AnalyticsEvent`, every failure path cleaning up the temp file); `POST /api/v1/video-edit/sources` (new, `authenticate`-only). One necessary fix caught mid-implementation: extending the shared `VideoAssetKind` enum silently widened `POST /video-assets/generate`'s accepted input, so its validator now explicitly excludes `UPLOADED_SOURCE` (listing/filtering still accepts it). Frontend: `VideoSourceUpload` (new panel inside the existing Video tab, upload progress, real `<video>` playback of the actual uploaded file — the first real video playback in this codebase), `VideoAssetList` gained a third card-rendering branch for real video-file kinds; one necessary fix to the shared `api.ts` axios client, whose request interceptor was unconditionally forcing `Content-Type: application/json` on every request — now skipped for `FormData` bodies, the first file upload anywhere in this frontend. 585/585 backend tests passing (23 new), typecheck/build clean on both sides, one migration applied with zero drift (`prisma migrate status` confirmed). Frontend typecheck/build/lint clean (repo-wide lint baseline unchanged, 27 errors/3 warnings before and after); full suite 353 tests, 349 passing, 4 failing — the same pre-existing, already-documented baseline failures (`search-provider.test.tsx` ×2, `user-auth-form.test.tsx` ×2), none in a file this sprint touched. **Live-validated** against the real EYAN Studio development database via both a direct API call and a full real-browser session (registration → sign-in → project creation → Video tab → file upload, using a real `ffmpeg`-generated test video) against a throwaway backend/frontend instance on spare ports — production/dev instances untouched throughout: confirmed correct `ffprobe` metadata, the file physically moved (not copied) into storage, correct static serving, automatic appearance in the Asset Library (`AssetType.VIDEO`, `DRAFT` status) and Review queue, and correct rejection of both a non-video file (ffprobe validation) and a wrong-mimetype upload (multer filter). Full detail in the sprint log.

**Sprint 7.1 — MCP Foundation (2026-07-27):** Built the platform-level integration layer Sprint 7's future providers (Canva first, per Sprint 7.2) will register into. Backend: `McpConnector` interface (`connect`/`listTools`/`callTool`/`disconnect`/`healthCheck`) + `McpConnectorFactory` registry + `FakeMcpConnector` (Milestone 2); repositories for the three new models, persistence-only (Milestone 3); `CredentialManagerService` (pure AES-256-GCM encrypt/decrypt/rotate, no repository dependency — the only thing in the codebase that touches the encryption key), `AutomationConnectionService` (Connection Manager — create/update/delete/enable/disable/rotate/reveal, every method ownership-checked), `McpHealthService` (orchestrates a connector's connect→healthCheck→disconnect lifecycle and persists the result), `AutomationAuditService` (standardized audit recording/querying, paginated) (Milestone 4); full REST API — DTOs/mappers that structurally omit `encryptedCredentials`/`credentialsIv` from every response, validators, controllers, routes wired into `app.ts`, plus one small necessary addition beyond the original service brief, `McpServerConfigService` (CRUD lifecycle for `McpServerConfig`, since nothing else owned it and controllers must go through a service) (Milestone 5); frontend `features/automation` module — 5 pages, React Query hooks, zod-validated forms, permission-gated actions mirroring the backend's actual RBAC gates route-for-route, new "Automation" sidebar section (Milestone 6). 57 backend test files / 562 tests passing (all new work this sprint), 66 frontend test files / 347 tests (343 passing, 4 pre-existing unrelated `user-auth-form.test.tsx` failures, confirmed via isolated re-run), typecheck/build clean on both sides, `prisma validate` clean, one migration applied with zero drift. Not live-validated against a running dev server end-to-end this sprint (a live boot smoke test confirmed clean startup with no wiring errors; interactive UI walkthrough deferred — see Open Risks). Full detail in the sprint log and `.claude/decisions/ADR-0012-mcp-foundation.md`.

**Sprint 6.6 — Production Dashboard (2026-07-27, live-validated same day) — Sprint 6 is now complete.** Built the dashboard UI on top of Sprint 6.5's Analytics Foundation. A new, dedicated Content Studio page (`/content-studio/dashboard`, new "Production Dashboard" sidebar entry alongside "Content Studio" and "Prompt Library") — **confirmed with the user to be separate from the platform's existing home dashboard**, since platform administration (system health/models/providers) and Content Studio creative operations are different domains for different users. The identical composition (`AnalyticsSummary`: assets-by-type and provider-usage `recharts` bar charts, review/publishing status count lists, review/publishing performance stat tiles) is reused unchanged as a new "Analytics" tab on `ProjectWorkspace`, since the backend returns the identical DTO shape for both project- and platform-scoped summaries. One small, additive backend extension: `GET /api/v1/analytics/activity` (platform-wide activity feed) via two new `findManyByProjectIds` repository methods and a shared `buildActivityFeed()` helper — no new migration, no new storage. Charts follow the `dataviz` skill: single-series magnitude bars use one consistent theme color (not a categorical palette, since there's only one series); a handful of named statuses render as a labeled count list (reusing `AssetStatusBadge`'s existing `Badge` variants) rather than a chart. 448/448 backend tests passing (up from 442), typecheck/build clean; frontend typecheck/build/lint clean (repo-wide lint unchanged from baseline), full suite runs to completion: 325/329 passing, 4 failing, all 4 the same pre-existing baseline failures (one was briefly a regression this sprint introduced — adding the "Production Dashboard" sidebar item made an existing test's non-exact `getByText('Dashboard')` assertion ambiguous — caught and fixed by making that assertion exact, not by avoiding the word "Dashboard" in the new feature). **Live-validated** against the real EYAN Studio development database and backend, including — for the first time this sprint — a real Playwright browser session driving the actual running dev-mode frontend (not just mocked component tests or API calls): registered a user, generated/approved/published a real asset, logged in through the real sign-in form, and visually confirmed both the Production Dashboard and the project's Analytics tab render live chart/tile/activity-feed data correctly via inspected screenshots. Full detail in the sprint log and `docs/ASSET_LIBRARY.md`.

**Sprint 6.5 — Analytics Foundation (2026-07-27, live-validated same day):** Added the data and query layer Sprint 6.6's Production Dashboard will visualize. Research confirmed almost every tracked metric (review workflow, approval metrics, publishing, publishing performance, brand kit usage, provider usage, generation duration) is already answerable by querying existing tables (`AssetReview`, `AssetReviewEvent`, `PublishingRecord`, and the `provider`/`model`/`generationTimeMs`/`brandKitId` columns already on `GeneratedContent`/`GeneratedImage`/`VideoAsset`) — with exactly one gap: a first-time generation writes zero event/log row anywhere today. One new, purely additive table, `AnalyticsEvent` (same append-only polymorphic `(assetType, sourceId)` shape as `AssetReviewEvent`, but deliberately a separate table to keep `AssetReviewEvent`'s review-scoped purpose intact), covers that gap — written **genuinely unawaited (fire-and-forget)** from `ContentService`/`ImageService`/`VideoAssetService.generate()`, a first inside a request path in this codebase (the only prior unawaited pattern runs once at server boot). A new read-only `AnalyticsService` composes existing repositories (plus three new, additive `findManyByProject()` methods on `AssetReviewEventRepository`/`AssetReviewRepository`/`PublishingRecordRepository`) into project and platform-wide summaries (asset counts by type, review/publishing status counts with an implicit `DRAFT` fold-in, provider usage with average generation duration, brand kit usage, review/publishing performance) and a unified, paginated activity feed merging `AnalyticsEvent`/`AssetReviewEvent` — no second timeline. Three new `authenticate`-only endpoints under `/api/v1/analytics/...`. **Sprint 6.5 ships backend-only**, matching the precedent Sprint 4.1 already set — Sprint 6.6 builds the dashboard UI on the already-installed, previously-unused `recharts` dependency. 442/442 backend tests passing (up from 384), typecheck/build clean; no frontend changes this sprint. **Live-validated** against the real EYAN Studio development database and backend: generated one real content asset and one real video-script asset via live Ollama (averaging ~152s each, consistent with this hardware's documented slow-generation profile) plus one fake-provider image, confirmed the summary/provider-usage/activity-feed endpoints reflected all three correctly, then ran the image through a full review→approve→schedule→publish cycle and confirmed review/publishing status counts and performance metrics updated correctly and the activity feed grew to include both generation- and review-sourced events — and confirmed all three generation calls returned `201` throughout, directly demonstrating the fire-and-forget requirement end-to-end rather than only in unit tests. Full detail in the sprint log and `.claude/decisions/ADR-0011-analytics-foundation.md`.

**Sprint 6.4 — Publishing Pipeline (2026-07-27, live-validated same day):** Added a Publishing Pipeline as an independent workflow from QA review — `ReviewStatus`/`AssetReview` are untouched. One new, purely additive table, `PublishingRecord`, keyed on `(assetType, sourceId, platform)` (`@@unique`, upsert-style like `AssetReviewAssignment`), with its own `PublishingStatus` lifecycle (`DRAFT/SCHEDULED/PUBLISHING/PUBLISHED/FAILED/ARCHIVED`), gated on but decoupled from `AssetReview.status === "APPROVED"` (checked at the service layer, not a database constraint). A new `PlatformProvider` abstraction mirrors `ImageProviderFactory`'s `Map`-based registry (`register`/`create`/`listRegistered`/`reset`) rather than `ProviderFactory`'s single-hardcoded-provider shape (ADR-0001), since real publishing targets (Facebook, Instagram, LinkedIn, TikTok, YouTube, WordPress) are known future candidates — only a deterministic `FakePlatformProvider` is registered today. Five new endpoints under `/api/v1/assets/:assetType/:sourceId/publishing...` (list, schedule/PUT, publish/POST, retry/POST, archive/DELETE), `authenticate`-gated only, matching every existing asset route. `AssetService` gained six new methods, all reusing the existing `findSource()` ownership check; `publish()` is the single, stable core method a future background executor could call unchanged. `list()`/`getDetail()` now batch-fetch and surface a `publishing: {platform, status}[]` summary; `GET /assets` gained a `publishingStatus` filter. Every publishing action feeds the existing `AssetReviewEvent` timeline (4 new event types) — no second timeline. Frontend: a new `components/publishing/` subfolder (`PublishDialog`, `PublishingStatusPanel`, `PublishingQueue` — a deliberate sibling to `ReviewQueue`, not a merged tab bar); `AssetDetailSheet.tsx` gained a Publishing tab, `ProjectWorkspace.tsx` gained a Publishing tab. **Confirmed with the user as the core scope decision**: scheduling is persisted intent only — no queue/cron/background worker exists in this codebase (confirmed by research) or was built this sprint; `scheduledFor`/`SCHEDULED` status just records intent, and only an explicit "Publish Now" (or a future admin action) actually executes a publish. 413/413 backend tests passing, typecheck/build clean on both sides; frontend typecheck/build/lint clean (repo-wide lint unchanged from baseline), full suite runs to completion: 308/312 passing, 4 failing, all 4 the same pre-existing baseline failures, none in a Sprint 6.4 file. **Live-validated** against the real EYAN Studio development database and backend, including exercising the failure path (an unregistered platform) which **caught and led to fixing a real bug**: `platformProviderFactory.create(platform)` was originally called outside the `try`/`catch` in `publish()`, so a provider-lookup failure left a record stuck at `PUBLISHING` instead of transitioning to `FAILED` — fixed by moving the call inside the `try` block, re-validated end-to-end afterward. Full detail in the sprint log and `.claude/decisions/ADR-0010-publishing-pipeline.md`.

**Sprint 6.3 — Creative Review Workspace (2026-07-27, live-validated same day):** Extended the Sprint 5 QA review system in place — no second review system. Three new, purely additive tables, all keyed on `(assetType, sourceId)` exactly like `AssetReview`/`AssetVersion`: `AssetComment` (general comments and annotations unified in one table — an annotation is a comment with a `region`/`timestampMs` anchor; `isInternal` marks reviewer-only notes), `AssetReviewAssignment` (**informational-only** reviewer assignment — records intent for review planning/queue organization/timeline history, grants zero project access), `AssetReviewEvent` (append-only activity log covering status changes, comments/annotations, resolutions, assignment changes, and new versions). `ReviewStatus` gained `REVISION_REQUESTED` (additive enum growth). Seven new endpoints under `/api/v1/assets/:assetType/:sourceId/...` (comments CRUD + resolve, assignment PUT/DELETE, timeline GET), `authenticate`-gated only, matching every existing asset route — no new RBAC permission was added, since this repo's global RBAC governs page-level access, not per-resource collaboration, and applies nowhere in the asset module today. `AssetService.review()`/`regenerate()` gained event side-effects; `list()`/`getDetail()` now batch-fetch and surface `commentCount`/`openCommentCount`/`assignee`. Frontend: a new `components/review-workspace/` subfolder (`CommentThread`, `ImageAnnotationOverlay` — click-drag rectangle drawing via plain positioned divs, no canvas dependency — `VideoTimestampAnnotations`, `AssigneePicker` reusing the existing admin `getUsers()`, `ReviewTimeline`, `RequestRevisionDialog`); `AssetDetailSheet.tsx` reorganized into Details/QA/Comments/Timeline tabs; `ReviewQueue.tsx` gained a Revision Requested filter, assignee/comment badges, and a "My assignments" toggle (the first Content Studio use of `useAuthStore`). The one consequential decision made mid-sprint, confirmed with the user before implementation: review assignment is informational only — this repo has no multi-user project access model (`ContentProject.userId`, ADR-0007, single-owner), so extending real access for an assignee would be a genuinely new access-control primitive, not an extension of the existing model, and was explicitly deferred to a future, dedicated multi-user/RBAC-expansion sprint. 384/384 backend tests passing (69 new), typecheck/build clean on both sides; frontend typecheck/build/lint clean (repo-wide lint unchanged from baseline), full suite runs to completion: 296/300 passing, 4 failing, all 4 the same pre-existing baseline failures, none in a Sprint 6.3 file. **Live-validated** against the real EYAN Studio development database and backend, including using a second real user's own auth token to confirm that being assigned as reviewer produces `404` on both the project and the asset endpoints — proving the informational-only decision actually holds, not just asserted. Full detail in the sprint log and `.claude/decisions/ADR-0009-creative-review-workspace.md`.

**Sprint 6.2 — AI Video Studio (2026-07-27, live-validated same day):** Added `VideoAsset` (owned directly by `ContentProject`, no `VideoProject` header entity) and `VideoAssetKind` (8 values: `SCRIPT`, `SCENE_BREAKDOWN`, `SHOT_LIST`, `VOICE_OVER_SCRIPT`, `CAPTIONS`, `SUBTITLES`, `STORYBOARD`, `THUMBNAIL`). Full generate/list/detail/delete under `/api/v1/video-assets`; a plain `videoGroupId` column groups related artifacts into "one video" with no second entity — omitted, a fresh one is generated server-side. The 6 text kinds call `ChatService`/`OllamaProvider` directly (no new provider registry — see Decisions below); the 2 image kinds (`STORYBOARD`/`THUMBNAIL`) reuse `ImageProviderFactory` unchanged, stage-for-stage identical to `ImageService.generate()`. Optional `brandKitId` reuses Sprint 6.1's `buildContentBrandGuidance()`/`buildImageBrandGuidance()` helpers unmodified. `AssetType.VIDEO` makes every artifact a real Asset Library entry automatically — unlike `BRAND_KIT`/`PROMPT_TEMPLATE`, **regenerate is supported** (a video asset, either kind category, is provider/AI-generated); duplicate dispatches to a row-copy or a re-generation depending on the source's kind. New "Video" tab in the project workspace (kind selector grouped Text/Visual, a new reusable `BrandKitSelect` component, an "attach to existing video" picker derived client-side from the already-fetched list, a grouped kind-badged output view). One key architectural decision made before implementation: the original plan's `VideoScriptProvider` registry (mirroring `ImageProviderFactory`) for the six text kinds was dropped after finding ADR-0001 (this repo's hardware is deliberately single-text-provider, no gateway) — building a registry that would hold exactly one entry was judged the same speculative abstraction ADR-0001 already rejected; confirmed with the user before coding. Also discovered and flagged (not fixed, follow-up): Sprint 6.1 wired `brandKitId` into the Content/Image generation *endpoints* but never added a picker to either generation *form* — `BrandKitSelect` was built reusably enough to retrofit later. 347/347 backend tests passing (27 new), typecheck/build clean on both sides; frontend typecheck/build/lint clean (repo-wide lint unchanged from baseline), full 46-file/280-test suite runs to completion: 276 passing, 4 failing, all 4 the same pre-existing baseline failures, none in a Sprint 6.2 file. **Live-validated** against the real EYAN Studio development database and backend: `prisma migrate dev` generated and applied the migration directly (no hand-authoring needed, unlike Sprint 6.1), `prisma migrate diff` confirmed zero drift; generated one real artifact of all 8 kinds (6 via live Ollama calls, including one with brand-kit guidance visibly folded into the output; 2 via a live Hugging Face provider call producing real image bytes), confirmed `videoGroupId` grouping, correct Asset Library integration, and regenerate/duplicate/review/version/QA-batch/delete all working correctly on `VIDEO` assets, plus a content-generation regression check. Full detail in the sprint log.

**Sprint 6.1 — Brand Kits (2026-07-26, live-validated 2026-07-27):** Added `BrandKit` (owned by `ContentProject`, one project → many kits) covering logos, primary/secondary colors, fonts, typography, tone of voice, writing style, audience, CTA style, approved terminology, restricted words, and free-text brand/image/social guidelines. Full CRUD under `/api/v1/brand-kits`. `ContentService.generate()`/`ImageService.generate()` each gained an optional `brandKitId` parameter — when supplied (and verified to belong to the same project), the kit's guidance is folded into the content system prompt / image prompt via a small shared helper (`brand-kit-guidance.ts`); omitted, both are byte-for-byte unchanged from before this phase. `AssetType.BRAND_KIT` makes every kit a real Asset Library entry automatically (list/search/filter, review, duplicate, delete; regenerate is explicitly unsupported, matching `PROMPT_TEMPLATE`) — `AssetReview`/`AssetVersion` needed zero schema changes. New "Brand Kit" tab in the project workspace (list/create/edit/delete), built with react-hook-form + zod on the established `role-dialog.tsx` pattern; logos/colors/fonts/terminology are entered as comma-separated text rather than a file-upload/color-picker/tag-input UI, since no upload middleware exists anywhere in this codebase yet — a deliberate, named scope simplification, not a schema compromise (see the sprint log's Decisions Made). One architectural correction made before implementation: the original plan's `VideoProject` header entity (for the not-yet-started Video Studio phase) was dropped in favor of `VideoAsset` hanging directly off `ContentProject`, reaffirming this repo's single-project-hierarchy rule. 320/320 backend tests passing (24 new), typecheck/build clean on both sides; frontend typecheck/build/lint clean, and — now running against a normal-sized `/dev/shm` instead of the prior sandbox's 64MB one — the full 43-file/262-test frontend suite runs to completion: 258 passing, 4 failing, all 4 the same pre-existing `search-provider.test.tsx`/`user-auth-form.test.tsx` baseline failures, none in a Sprint 6.1 file. **Live-validated 2026-07-27** against the real EYAN Studio development database and backend: `prisma migrate deploy` applied the hand-authored migration with no modification needed, `prisma migrate diff` confirmed zero schema/database drift, and a full live pass covered Brand Kit CRUD, brand-guided content generation (output visibly reflected the kit's tone/terminology/CTA-style/guidelines), brand-guided image generation (completed via the Hugging Face provider), correct Asset Library/`AssetType.BRAND_KIT` integration, unchanged Asset Review/Version behavior on both existing and new asset types, and a full regression pass on every pre-existing flow touched. Full detail in the sprint log.

**Sprint 5 — Enterprise Asset Library & QA Workflow (2026-07-25):** Added an Asset Library (Assets tab) and QA review workflow (Review tab) to every project's workspace, aggregating `GeneratedContent`, `GeneratedImage`, and (newly) project-scoped `SavedPrompt` rows into one unified, searchable, filterable, paginated, batch-actionable view — `GET /api/v1/assets` merges up to 500 rows from each source in parallel, in application code (a documented, deliberate scale boundary — see `docs/ASSET_LIBRARY.md`). QA review (`AssetReview`, new table, keyed on `(assetType, sourceId)`, lazily created) supports Draft/Needs Review/Approved/Rejected/Published, a reusable Pass/Fail/Comment checklist with separate content vs. image item sets, reviewer notes, and a manually-entered QA score. Regenerating an asset reuses `ContentService.generate()`/`ImageService.generate()` completely unchanged, then links the new row into `AssetVersion` (new table, self-referential lineage) for Version History and a side-by-side Compare view; Duplicating creates an independent copy (a true instant row copy for content/prompt templates, a fresh regeneration for images, since no file-copy primitive exists) with no version link. `SavedPrompt.projectId` (nullable, additive) lets a prompt be scoped to a project via a new "New Prompt Template" action — reusing the existing `SavePromptDialog` with a `projectId` prop, zero duplicated form logic — without changing the standalone Prompt Library page's existing global-prompt behavior at all. The one necessary touch to already-live generation code: `generationTimeMs` (nullable, additive) was added to `GeneratedContent`/`GeneratedImage`, captured by timing the existing provider call, since Asset Details needs to show generation time and it can't be reconstructed after the fact. Downloads/exports (Markdown, JSON, image files) are entirely client-side, per an explicit scope decision — no new export endpoints. The Review Queue is scoped per-project this sprint (a tab, not a route) but built on the same `useAssets(projectId, filters)` hook the Asset Library uses, with `projectId` already a normal, relaxable query parameter on the backend — promoting it to a global, cross-project queue later needs no rewrite. Frontend filter/pagination/batch-select UI is visually modeled on the existing (previously unused) `components/data-table/*` kit but implemented as purpose-built, plain-props siblings rather than wrapping it directly, since that kit is bound to `@tanstack/react-table`'s client-side filtering and this feature filters server-side — documented in `docs/ASSET_LIBRARY.md`. 296/296 backend tests passing (55 new: `asset.service.test.ts` is the main new surface, DI-faking every dependency in the established `image.service.test.ts` style), 253 frontend tests (46 new, all passing), typecheck/lint/build clean on both sides, repo-wide lint unchanged from its pre-existing baseline (26 errors/3 warnings). Live-validated end-to-end against a throwaway backend on a spare port: aggregation of all three asset types, type/search filtering, a full review-then-regenerate-then-verify-version-2 sequence (confirming the original image kept its `APPROVED` status independently of the new version), a mixed-outcome batch action (2 succeeded, 1 correctly failed without aborting the batch), and a full regression check of every pre-existing endpoint (`/content`, `/images`, `/saved-prompts`) — production untouched throughout. Full detail in the sprint log.

**Sprint 4.4 — Hugging Face Image Provider (2026-07-24):** Added `HuggingFaceProvider`, using Hugging Face's official Inference Providers API (`hf-inference` — Hugging Face's own first-party serverless infrastructure, not a third-party-routed provider or unofficial endpoint), registered alongside `fake`/`gemini`/`comfyui` with zero changes to `ImageService`, routes, or validators. `HUGGINGFACE_MODEL` is the sole source of truth for which model is called — `model.config.ts` only supplies a default and a structural `namespace/model-name` shape check, never a hardcoded assumption that a specific model is actually available to the configured account; an inaccessible/nonexistent model surfaces as a specific, logged 403/404, not a crash or silent fallback. Like `GeminiImageProvider`/`ComfyUIProvider`, it never sanitizes its own errors — `response.mapper.ts`'s `describeHuggingFaceError()` turns a raw HTTP failure (401/403/404/429/503/timeout/network) into one clear, specific message, still thrown as a plain `Error` so `ImageService`'s Phase 0 stage-based handling remains the single place that produces the safe client-facing message. Its health check (`checkHuggingFaceHealth()`) validates auth (`GET /api/whoami-v2`) and model access (`GET /api/models/{id}`) via Hugging Face's Hub metadata API — free, no generation, satisfying "health checks must not generate images" while still being a real network check, not just a config-shape one. `randomSeed()` (needed by both `ComfyUIProvider` and now `HuggingFaceProvider`, since neither has a request-level seed field) was factored into one shared `random-seed.util.ts` rather than duplicated. Frontend: the provider selector now includes Hugging Face and Fake explicitly (five options total), and gained provider-tailored pending-state hints and error-message prefixes — both purely client-side, using data the form already has, with no backend changes. 223/223 backend tests passing (55 new), 197/201 frontend tests passing (10 new, all passing), typecheck/lint/build clean on both sides, live-validated against a throwaway backend instance including a real (free, zero-cost) call to Hugging Face's own Hub API that correctly detected an intentionally-invalid token. Full detail in the sprint log.

**Sprint 4.3 — ComfyUI Image Provider (2026-07-24):** Added `ComfyUIProvider`, a self-hosted `ImageProvider` talking to ComfyUI's standard REST API (`POST /prompt` → poll `GET /history/{id}` → `GET /view`), registered alongside `fake`/`gemini` with zero changes to `ImageService`, routes, or validators — same non-negotiable as Gemini's own integration. Generation parameters are injected into user-authored JSON workflow templates (`backend/resources/workflows/*.json`) via `{{PLACEHOLDER}}` substitution (`workflow.loader.ts` + `workflow.mapper.ts`), so new ComfyUI workflows never require backend code changes; no ComfyUI node id is ever hardcoded — the result image is found by scanning `/history`'s output nodes for the first one with an image, not by assuming a specific node. Like `GeminiImageProvider`, it never sanitizes its own errors, preserving Phase 0's raw-detail-to-owner / safe-message-to-client separation. Config (`COMFYUI_URL`/`COMFYUI_WORKFLOW`/`COMFYUI_TIMEOUT`/`COMFYUI_POLL_INTERVAL`) is fail-fast validated at boot (synchronous, network-free, matching this codebase's existing pattern); a separate, non-fatal `logComfyUIHealthCheck()` also runs at boot to report live reachability without blocking startup on a network call. Shipped alongside: a minimal "Images" tab in Content Studio's existing project workspace (provider selector persisted to localStorage, prompt input, generated-image display with a provider badge, sanitized server error messages surfaced directly since Phase 0 already guarantees they're safe to show) — the negotiated-down scope of the sprint's original "integrate into the existing image generation UI" ask, since no such UI existed yet. Discovered mid-sprint and fixed: generated images were never actually servable over HTTP (see Current Sprint above). 219/219 tests passing (43 backend + 12 frontend, all new), typecheck/lint/build clean on both sides, fully live-validated (fake-provider generation + the new static file serving + comfyui's graceful, sanitized failure against an intentionally-unreachable ComfyUI instance) against a throwaway backend instance — production untouched throughout. Full detail in the sprint log.

**Sprint 4.2, Phase 1 — Gemini Image Provider Integration (2026-07-24):** Added `GeminiImageProvider`, using Google's official `@google/genai` SDK, as the first real `ImageProvider` implementation — plugging into the registry Sprint 4.1 built with zero changes to `ImageService`, routes, controllers, or validators. It deliberately does not sanitize its own errors, letting them propagate to `ImageService`'s existing Phase 0 stage-based handling, so the raw-detail-preservation work that phase did isn't silently undone by the first real provider. Maps requested width/height to the nearest of Gemini's supported aspect ratios (arbitrary pixel dimensions aren't supported); ignores `negativePrompt` (no native equivalent) rather than inventing a prompt-engineering workaround; always reports output format as `png` (the only format Gemini produces), which surfaced — but didn't cause — a pre-existing gap where `ImageService` never reconciles a provider's actual output format against the format it persisted before generation (flagged as a follow-up, not fixed here, since fixing it means touching `ImageService`). `validateGeminiProviderConfig()` fails the process at boot if `IMAGE_PROVIDER=gemini` but `GEMINI_API_KEY` is unset, verified live (immediate crash, exit 1, before any server binds). 125/125 tests passing (17 new, `@google/genai` fully mocked — no real network calls in the suite), typecheck and build clean. No real, billed Gemini API call has been made in production or otherwise — treated the first real call as worth asking about first, not assuming. Full detail in the sprint log.

**Sprint 4.2, Phase 0 — Production Readiness (2026-07-24):** Hardened the Sprint 4.1 pipeline without changing its architecture, framed explicitly around this project's purpose as an AI Content Production / Content Moderation portfolio. `validateLocalDiskStorageConfig()` now fails the process at boot if the storage root isn't writable, mirroring the existing provider-config fail-fast check. `ImageService.generate()`'s error handling now attributes failures to a specific stage (initial record creation, provider call, storage call, final status update) instead of one generic catch-all, and — the most consequential fix — no longer forwards raw underlying error text (which could include vendor/internal detail) to the HTTP client; a fixed, safe message is thrown instead, while the full detail is still persisted to the owner-visible `errorMessage` column. Every lifecycle event (generation start, provider success/failure, storage success/failure, completion, deletion, provider resolution) now has a corresponding log line. All of it verified live against a throwaway instance, not just asserted in tests: an unwritable storage root crashes the process at boot with a clear message, and real log output was inspected directly to confirm the expected sequence fires. 108/108 tests passing (10 new), typecheck and build clean. Full detail in the sprint log.

**Sprint 4.1 — AI Image Studio, Backend Pipeline (2026-07-24):** Five independently-reviewed phases built a complete internal image-generation pipeline before any real, billable AI provider was integrated. `GeneratedImage` hangs off `ContentProject` with ownership derived transitively (no duplicate `userId` column, extending ADR-0007). `ImageProvider` and `StorageProvider` are independent abstractions — a provider only ever produces bytes, storage only ever persists bytes it's handed, `ImageService` is the sole orchestrator. Unlike the single-provider `AIProvider` (ADR-0001, hardware-constrained), `ImageProviderFactory` is a real registry, since multiple hosted image providers are genuinely expected next. The full request → generate → store → persist → retrieve → delete cycle was proven correct using a deterministic, in-process `FakeImageProvider` — including failure paths (provider failure, storage failure, a DB-layer failure occurring *after* a successful generation, which a Phase 5 fix stopped from being mislabeled as a failed generation) — validated with real HTTP requests against a throwaway instance on a separate port, production never touched. 98/98 tests passing (46 new this sprint), typecheck and build clean throughout. Full detail in the sprint log.

**Sprint 3.5 — Security Hardening (2026-07-24):** `ContentProject` gained required, enforced `userId` ownership (ADR-0007), following the exact pattern ADR-0006 established for `SavedPrompt`. `GeneratedContent` ownership is derived transitively through its parent project rather than a duplicated column. All affected routes now scope reads/writes to the requesting user and return `404` (not `403`) for rows that exist but aren't theirs. Migration backfilled the 3 pre-existing production `ContentProject` rows against real data (not guessed). Added 46 new unit tests for previously-untested `projects`/`content` service and repository layers (52/52 passing). Deployed via the standard `deploy.sh` flow and validated live in production against two disposable test accounts, covering project creation, listing, content generation, content history, and cross-user access on read and delete paths — all correct, backend logs clean. One operational note: a brief live regression window occurred when the migration was applied directly to production ahead of the code deploy (the environment was initially mistaken for non-production); no data loss, closed within the same session. Full detail in the sprint log.

Sprint 3 delivered a minimum-viable Prompt Library end-to-end (`SavedPrompt` backend with enforced per-user ownership per ADR-0006, plus a full frontend), a Content Studio polish pass (layout consistency, Generation History collapse/delete-confirm), and — following the real production deployment — resolved a three-layer production incident.

**Production incident, now fully resolved.** The first real production deployment of this sprint's work surfaced a chain of three independently-configured timeouts that had never been reconciled against each other or against real generation time on this hardware: the frontend's Axios client (30s → 180s), the backend's `OllamaProvider` client (180s → 300s, after production traffic showed a cold model load can exceed 180s), and nginx's `/api/` reverse-proxy timeout (unconfigured, defaulting to 60s → explicitly set to 330s). Each was fixed in turn as it was exposed by the one before it. `deploy.sh` was also enhanced to automatically warm the configured Ollama model right after every deploy's health check passes, so future deploys never expose the cold-start condition that triggered this incident in the first place. Every fix in this chain was validated with a real, live request against production (not just log inspection or configuration review) before being considered resolved — full timeline, root-cause chain, and evidence in `tasks/completed/sprint-3-prompt-library.md`.

Production is confirmed healthy and running all of Sprint 3's work, including this incident's fixes, as of this update.

Sprint 3's closeout also re-measured project-wide validation and corrected two inaccuracies that had been repeated across prior sprint logs: frontend lint is actually 26 pre-existing errors / 3 warnings repo-wide (not "2"), and the 4 pre-existing test failures are split across `user-auth-form.test.tsx` (2) and `search-provider.test.tsx` (2), not all in the former.

Sprint 2 is complete and closed (see `tasks/completed/sprint-2-templates-and-picker.md`). Foundation Sprint (AI Collaboration Framework, Stage 1) also completed earlier in this line of work — see `tasks/completed/foundation-sprint-ai-collaboration-framework.md`.

---

## Next Task

**CRM Foundation Sprint 1 and Sprint 2 (Automation Integration Contract) are both complete.** Sprint 2 built only the EYAN side of the contract (`authenticateService`, signed webhook dispatch, `/crm/service/*` write-back endpoints, idempotency) — `eyan-automation-hub` itself (n8n's real Workflow 1/2 JSON definitions) was explicitly deferred, by the user's own direction, to its own dedicated sprint, not yet started or approved. Real AI qualification (Sprint 3, replacing this sprint's dummy/stub qualification payload with an actual Ollama call) is the natural next step after that Automation Hub sprint, per the approved TDD. This track is independent of both the Finance and Sprint 7 tracks below.

**Finance Management Sprint A (Dashboard + Expenses) is complete.** Sprint B (Bills, `RecurringBillTemplate`, a `$transaction`-backed mark-paid flow — this codebase's first `$transaction` usage) is the natural next step per the approved Technical Design Document, and has not been started. Sprint C (Savings goals/contributions, a second `$transaction` usage, and the Finance Settings page — currently the only way to create a `RecurringExpenseTemplate` is the Expense dialog's "Repeat monthly" toggle; there is no UI yet to edit or deactivate one) and Sprint D (Reports export, receipt upload) follow. This track is independent of the Sprint 7 AI Video Editing Pipeline work below.

**Sprint 7.2.1 (Source Ingestion), Sprint 7.2.2 (Workflow Planner), Sprint 7.2.3 (Execution Engine + Core FFmpeg Operations), and Sprint 7.2.4 (AI Transcription & Subtitle Generation) are all complete.** Sprint 7.2.5 (computer-vision operations — `blur_faces`, `auto_zoom` via a new `OpenCvVideoAnalysisProvider`, sequenced last with an explicit go/no-go checkpoint on this hardware's real performance) is the natural next step per the approved AI Video Editing Pipeline architecture, and has not been started or approved. Real, interior-segment `remove_silence` (beyond Sprint 7.2.3's leading/trailing-only MVP) is also a candidate to revisit, now that Whisper is integrated — not done this sprint, since it wasn't asked for and `subtitles` was the named 7.2.4 scope. The full milestone order beyond that: 7.2.6 (polish, job history, `background_music`, `shorts`, live validation). A real Canva MCP Provider (previously assumed to be Sprint 7.2, now superseded — see Current Sprint) remains valid, un-started future work: `McpConnectorFactory.register()` is a one-line addition, `AutomationConnection` already supports generic credential storage a Canva OAuth token can use immediately, and `CredentialManagerService`/`AutomationAuditService`/`McpHealthService` need no changes to support a real connector. The remaining Sprint 7 roadmap beyond that, per the Sprint 7 architecture review that originally scoped MCP work: further MCP connectors (GitHub, Docker, Filesystem, Google Drive, Slack, Discord, Notion, PostgreSQL, Redis), OAuth authorize/callback flows for OAuth-based providers, n8n integration (workflow definitions/executions, a separate concern from the MCP Foundation), and workflow templates/execution UI — each a candidate for its own sub-sprint, none started.

Sprint 6 (Enterprise Creative Production Suite) is fully complete and live-validated — all six phases shipped; see the Sprint 6 entries below for full history.

Every prior phase's own follow-ups remain candidates whenever Sprint 7 (or an interim maintenance pass) picks them up: per-platform publishing performance (Sprint 6.5, currently computed per-asset, not per-platform, since `AssetReviewEvent` doesn't carry `platform` as a first-class column); a cross-user, admin-facing analytics view (Sprint 6.5, the natural place to finally use the existing, currently-unused `"analytics"`/`"dashboard"` seeded permissions); real background execution for scheduled publishing (Sprint 6.4, a queue/poller/cron-triggered endpoint calling the existing `AssetService.publish()` core) and real platform integrations (Facebook, Instagram, LinkedIn, TikTok, YouTube, WordPress) registered against `PlatformProviderFactory`; real project collaboration/membership (Sprint 6.3, the dedicated multi-user/RBAC-expansion sprint that would let an `AssetReviewAssignment` assignee actually access the project they're assigned to), a "Reviewer" RBAC role, and threaded comment replies; retrofitting `BrandKitSelect` onto the Content/Image generation forms (Sprint 6.2), further `VIDEO_SYSTEM_PROMPTS` iteration, and a `GET /video-assets/groups` aggregation endpoint. Sprint 5's own explicitly out-of-scope items (see `docs/ASSET_LIBRARY.md`) also remain candidates: a global cross-project Review Queue (the hook/API already support it), Version Restore, automated QA scoring, and server-side export if bulk/ZIP/PDF needs ever arise. `tasks/backlog/sprint-4-2-provider-integration.md`'s provider order (OpenAI → Stability → Gemini → FLUX) should be treated as historical, not a live plan — Gemini/ComfyUI/Hugging Face all shipped out of that order; its architecture readiness review remains accurate for whichever image provider comes next. Two concrete, small, standing follow-ups are ready to pick up any time (see Open Risks): the production nginx `/uploads/` proxy addition, and deploying Sprint 4.1–6.6 to production.

---

## Open Risks / Known Issues

- **`eyan-automation-hub` itself still doesn't exist for CRM** (Sprint 2) — n8n's real Workflow 1 (Lead Intake) and Workflow 2 (Validation) JSON definitions are not built; this sprint's outbound webhook dispatch and inbound `/crm/service/*` endpoints were verified with curl standing in for n8n, not a live Automation Hub instance. A repo-boundary conflict against the original "do not touch eyan-automation-hub" constraint was surfaced and the user confirmed EYAN-side-only for this sprint — the Automation Hub implementation is its own future sprint. See `.claude/decisions/ADR-0019-automation-integration-contract.md`.
- **No real AI qualification exists yet** (Sprint 1–2) — `PATCH /crm/service/leads/:id/qualification` is fully built and tested, but every qualification result to date has been a dummy/stub payload sent via curl; no Ollama call, no prompt template, no schema-validation-with-retry logic exists. The Human Review Queue / confidence-tiering described in the TDD is designed but not enforced anywhere — `LeadAiAnalysis.needsManualReview` is stored and returned but nothing in the UI or API acts on it yet. Deferred to Sprint 3, a named follow-up.
- **No DB-level uniqueness backs the idempotency check** (Sprint 2) — `CrmAutomationIngestService` looks up `WorkflowExecutionLog` by `{ workflowName, n8nExecutionId }` before writing (app-level, query-then-write), not a unique constraint; a genuine race between two concurrent calls with the same execution id could both pass the check before either writes. Acceptable given n8n calls sequentially per lead (no concurrent caller exists yet); revisit only if a real duplicate-write incident is observed. See ADR-0019 Decision 5.
- **No database transaction spans a status/assignment change and its paired `LeadActivity` write** (Sprint 1) — two sequential repository calls, not one `$transaction`; no cross-repository transaction pattern exists anywhere in this codebase yet, and introducing one for this single call site was judged out of scope. Acceptable at v1's write-concurrency (a human clicking a button); revisit if a real partial-failure incident occurs. See ADR-0018 Decision 3.
- **No UI exists for the public Lead Form itself** (Sprint 1) — `POST /api/v1/crm/leads` is fully built, rate-limited, and tested, but no external-facing marketing/capture page was built this sprint (not listed in the sprint's frontend deliverables); verified via curl/tests only.
- **Finance is a shared workspace with no per-user data isolation** (Sprint A) — a deliberate, explicit product decision (confirmed with the user before implementation), not an oversight: any user holding the `finance` permission can see and edit every expense/budget row; `createdBy`/`updatedBy` and the new `FinanceAuditEvent` log exist for audit only, never for access control. Future-proofed via an unused, nullable `householdId` column on every Finance table, but multi-household support is not implemented.
- **No recurring-template management UI exists yet** (Sprint A) — a `RecurringExpenseTemplate` can only be created via the Expense dialog's "Repeat monthly" toggle; there is no way yet to view, edit, or deactivate one. Deferred to the Sprint C Finance Settings page, a named follow-up, not a bug.
- **Receipt uploads are not yet implemented** (Sprint A) — `Expense.receiptPath` exists in the schema and is unused; the Multer/`LocalDiskStorageProvider` extension (adding `pdf` to the allowed-extensions set) is deferred to Sprint D, following the existing video-upload pattern.
- **A single 100%-value category collapses Recharts' `Pie` arc math to nothing** — confirmed a genuine Recharts limitation (not Finance-specific) during Sprint A's browser verification; worked around with a plain-SVG two-circle donut for that one case (see `.claude/decisions/ADR-0013-finance-management-foundation.md`). Any future single-series-capable Pie chart elsewhere in this codebase should apply the same fallback.
- **Subtitles are burned in as a permanent overlay only, no soft/toggleable track** (Sprint 7.2.4) — a named simplification, not built this milestone; a natural fast-follow once needed.
- **No hard timeout on the Whisper transcription subprocess** (Sprint 7.2.4) — a very long source video could in principle transcribe for a long time; the execution engine still has no per-step timeout for any operation (the same standing gap Sprint 7.2.3 already flagged for FFmpeg itself).
- **First-run model download requires network access** (Sprint 7.2.4) — `faster-whisper`'s configured model (default `small`) downloads from Hugging Face Hub on first use in any environment (a few hundred MB, cached afterward); documented in `backend/python/README.md`, not previously a concern since no prior milestone used Python/ML dependencies.
- **`remove_silence` only trims leading/trailing silence, not interior silent segments** (Sprint 7.2.3) — a named MVP simplification: removing interior silence too needs a second pass (concat demuxer stitching non-silent segments back together). Now that Whisper is integrated (Sprint 7.2.4), real transcript-based interior silence detection is a viable candidate to revisit — not done this sprint, since it wasn't asked for.
- **No idempotency guard on re-executing an already-executed workflow plan** (Sprint 7.2.3) — `VideoWorkflowPlanRepository.markExecuted()` is last-write-wins; running the same plan twice creates two separate `EDITED_VIDEO` assets and the plan's `resultVideoAssetId` simply points at whichever ran most recently. Not asked for this milestone.
- **No hard timeout on a single FFmpeg execution** (Sprint 7.2.3) — the five supported operations all have Zod-bounded parameters, but nothing yet kills a runaway or unexpectedly slow ffmpeg process; only small test videos have been exercised live so far (the same standing gap Sprint 7.2.1 already flagged for upload size).
- **No listing/history endpoint for generated workflow plans** existed at the end of Sprint 7.2.2 — resolved this sprint: `GET /api/v1/video-edit/planner?projectId=` now lists them, since the Execution Engine's "select an existing workflow" UI needed it.
- **The retry-once behavior has only been exercised live for the success path** (Sprint 7.2.2) — the real Ollama model produced a valid plan on the first attempt every time it was tried live; retry-after-malformed-JSON and retry-after-validation-failure are covered by unit tests with a mocked `ChatService` only, not yet observed against a real model's actual malformed output.
- **`background_music`'s `volume` parameter is planning-only** (Sprint 7.2.2) — which actual second audio file gets mixed in is unresolved until a real audio-asset upload path exists; that's an Execution Engine-era (7.2.3+) concern, not this milestone's.
- **Asset Library's grid thumbnail doesn't account for real video files yet** (Sprint 7.2.1) — `thumbnailUrl` just points at `storagePath` for any completed asset with one, which now includes an `UPLOADED_SOURCE` video file; the Asset Library grid renders a broken-image icon for it (the Video tab's own list already renders a real `<video>` player correctly, since that was in scope for this milestone and Asset Library's thumbnail rendering wasn't). Named, not fixed — fixing it means touching Asset Library's mapper/frontend rendering.
- **No disk-space quota/retention policy for uploaded video files** (Sprint 7.2.1) — the same standing risk this repo already carries for orphaned image files, now materially bigger in practice since video files are larger. `VIDEO_UPLOAD_MAX_BYTES` bounds a single upload (default 500MB) but nothing bounds total accumulated storage.
- **Only a small test upload has been exercised live** (Sprint 7.2.1) — a real `ffmpeg`-generated 45KB/3-second file was uploaded and validated end-to-end (API + real browser session), but `VIDEO_UPLOAD_MAX_BYTES`'s real 500MB ceiling and the PM2 500MB process-memory ceiling it was sized against have not been tested with an actual large file.
- **Only a fake MCP connector is registered** (Sprint 7.1) — `McpConnectorFactory` mirrors `ImageProviderFactory`'s pluggable registry shape, but no real provider (Canva, GitHub, Docker, Slack, ...) is integrated yet; every "connect"/"health check" today is a deterministic, no-network-call simulation. Adding a real connector is one new class + one `register()` call, no changes to any service or route — a named follow-up, starting with Sprint 7.2 (Canva).
- **No OAuth flow exists yet** (Sprint 7.1) — `AutomationConnection` only supports storing a generic, already-known credential payload (e.g. a pasted API key); authorize/callback routes for OAuth-based providers (GitHub, Slack, Discord, Notion, Google Drive) don't exist yet. The storage/encryption layer underneath is already OAuth-ready (`status`, `lastVerifiedAt`), only the auth flow itself is missing.
- **No stdio-process spawn/teardown lifecycle exists** (Sprint 7.1) — `FakeMcpConnector` is a no-op, in-process connector; a real STDIO-transport connector (the expected shape for Filesystem/GitHub/Postgres MCP servers, typically `npx`-invoked) needs a process-lifecycle layer this sprint didn't need to build yet.
- **Sprint 7.1's MCP Foundation was not live-validated against a running dev server end-to-end** — full backend/frontend test suites pass, typecheck/build are clean, and a boot smoke test confirmed the server starts cleanly with the new routes wired in, but an actual interactive walkthrough (create a connection, register an MCP server, trigger a health check, confirm the audit log, all via the real UI against a live backend) wasn't performed this sprint, unlike the live-validation Sprint 4–6 phases consistently did. Recommend doing this before Sprint 7.2 builds on top of it.
- **`McpServersPage`/`HealthPage` have no dedicated frontend test files** (Sprint 7.1) — they follow the identical pattern already covered by `ConnectionsPage`'s/`ProvidersPage`'s test files; a real but low-priority coverage gap, not a defect.
- ~~Sprint 6.1's migration has never been applied to or exercised against a real database~~ — resolved 2026-07-27: verified against the real EYAN Studio development database (`prisma migrate deploy` applied it with no modification needed; `prisma migrate diff` confirmed zero drift). See Status above and the sprint log.
- ~~The full frontend browser-mode test suite cannot run to completion in this sandbox (64MB `/dev/shm`)~~ — this was specific to the earlier sandboxed environment. Running locally (5.9G `/dev/shm`) as of 2026-07-27, the full 43-file/262-test suite completes cleanly in ~77s with no crash; 4 pre-existing, already-documented baseline failures remain (see below), none new.
- **Production nginx doesn't yet proxy `/uploads/` to the backend** — Express now correctly serves generated images (fixed this sprint), but nginx's `eyan.fyi` site config only proxies `/api/`, so a generated image's URL would still 404 in production until a one-line `location /uploads/ { proxy_pass ...; }` block is added manually (sudo-gated, outside version control — see `docs/COMFYUI_SETUP.md#5-production-serving-generated-images` for the exact snippet). Local dev is unaffected.
- **Sprint 4.1 through Sprint 6.6's application code is still not deployed to production** — Sprint 6.1's through 6.6's migrations and full functionality are now all verified against the real development database (see above), but `deploy.sh` still hasn't run since Sprint 3.5, and prior sprints' migrations (including Sprint 5's `AssetReview`/`AssetVersion`/`SavedPrompt.projectId`/`generationTimeMs` additions) are live in production while this development-validated code is not — recommend deploying (and adding the nginx block above) before any further work begins, so nothing builds on an untested deploy of eight sprints' worth of undeployed code.
- **Review assignments are informational only — no real multi-user project access exists** (Sprint 6.3) — assigning a reviewer records intent (queue organization, timeline history) but the assignee gains no ability to view or act on the project; `ContentProject` remains single-owner (ADR-0007). Live-validated as working-as-designed, not a bug. Real project collaboration is a named follow-up for a future, dedicated multi-user/RBAC-expansion sprint.
- **Publishing scheduling has no automatic execution — confirmed scope decision, not a gap** (Sprint 6.4) — a `SCHEDULED` `PublishingRecord` sits indefinitely until a user (or a future admin action) explicitly triggers it; no queue/cron/worker infrastructure exists anywhere in this codebase. `AssetService.publish()` is deliberately the single, stable core a future background executor could call unchanged, but building that executor is a named follow-up for a future sprint, not this one.
- **Only a fake publishing provider is registered** (Sprint 6.4) — `PlatformProviderFactory` mirrors `ImageProviderFactory`'s pluggable registry shape, but no real platform (Facebook, Instagram, LinkedIn, TikTok, YouTube, WordPress) is integrated yet; every "publish" today is a deterministic, no-network-call simulation. Adding a real platform is one new class + one `register()` call, no changes to `AssetService` or its routes — a named follow-up.
- **Publishing performance analytics is computed per-asset, not per-platform** (Sprint 6.5) — `AssetReviewEvent` doesn't carry `platform` as a first-class column (only inside `metadata`), so an asset published to two platforms in overlapping windows could have its `PUBLISH_STARTED`/`PUBLISHED` timing pairs computed across both indiscriminately. Accepted as a "Foundation"-level approximation, named as a follow-up.
- **The `"analytics"`/`"dashboard"` seeded RBAC permissions remain unused** (Sprint 6.5/6.6) — the new analytics/dashboard endpoints and pages stay `authenticate`-only, consistent with every other Content Studio route (see ADR-0011); these permissions are available for a future cross-user, admin-facing analytics view, not needed for the current per-owner rollups.
- **The Production Dashboard's platform-wide views loop/merge across every project a user owns in the service layer** (Sprint 6.6, same posture `AssetService.list()` and `AnalyticsService.getPlatformSummary()` already established in 6.5) — fine at demo/portfolio scale (a handful of projects per user), not a database-level rollup.
- **Asset Library aggregation is in-memory, not a database-level query** — `AssetService.list()` fetches up to 500 rows per source and merges/filters/paginates in application code, a documented, deliberate scale boundary (see `docs/ASSET_LIBRARY.md`). Fine at portfolio/demo data volumes; would need a database-level `UNION ALL` (or materialized view) before it could handle a real production catalog.
- **Version Compare is side-by-side, not a line-level diff; Version Restore is present but disabled** (future-ready per the Sprint 5 brief, not wired). **QA Score is a plain manually-entered number**, not computed from the checklist.
- **Video Studio's `SUBTITLES`/`CAPTIONS` have no real timestamp/timing sync** (Sprint 6.2), and **video-timestamp annotations (Sprint 6.3) are manually-entered logical markers, not synced to playback** for the same reason — no actual video file exists yet to sync against; both are named, explicit scope simplifications, not silent gaps.
- **Duplicating an image re-generates it rather than copying the file** — there's no file-copy primitive on `StorageProvider`, and two `GeneratedImage` rows pointing at one stored file would make deleting either one corrupt the other. This means an image "Duplicate" isn't byte-identical to the original if the provider is non-deterministic; documented, not treated as a bug.
- **No real ComfyUI instance has ever been exercised**; no ComfyUI instance has been stood up anywhere. `IMAGE_PROVIDER` is still `fake` in production. Gemini and Hugging Face, previously untested against real endpoints, were both exercised for real during Sprint 6.1 validation (2026-07-27): Gemini authenticated successfully but returned a `429 RESOURCE_EXHAUSTED` (free-tier quota exhausted, an account/billing state, not a code defect — the request reached the provider correctly); Hugging Face completed a full real, successful, billable-tier generation end-to-end (`black-forest-labs/FLUX.1-schnell`, image persisted to disk, `COMPLETED` status). Recommend a real ComfyUI smoke test, and revisiting Gemini once its quota/billing is addressed, before either is used for anything user-facing in production.
- **Known metadata gap, exposed by Gemini, not fixed**: `ImageService.generate()` persists the requested `format` before generation and never reconciles it against what a provider actually returned. `GeminiImageProvider` always produces PNG regardless of what was requested, so a `jpg`/`webp` request against Gemini will store a DB row whose `format` doesn't match the actual file on disk. `ComfyUIProvider` and `HuggingFaceProvider` both derive format from the actual output (filename extension / Content-Type header respectively), so neither is affected the same way, but the underlying gap in `ImageService` is unchanged. Not fixed, since fixing it means touching `ImageService`, out of scope for every phase that's exposed it so far.
- No rate limiting or per-user/per-project generation caps exist for image generation. Still harmless today (no real call has succeeded yet on any provider), but a hard prerequisite before any real, billable/resource-intensive provider goes live in production — flagged again here so it isn't lost.
- No retention/cleanup policy for orphaned local-disk files (e.g. a crash between a successful storage write and the following DB write). Narrow window, not yet hardened against.
- **Image Studio's frontend is a minimal single-generation slice, not a full feature** — no gallery/history list, no delete UI, no regenerate/download. It reuses `GeneratedImageItem` shaped exactly like the backend's `ImageResponseDto`, and `resolveImageUrl()` hardcodes the same `/uploads/images` path convention as the backend's `STORAGE_PUBLIC_BASE_URL` default — the two aren't otherwise linked, so changing one without the other would silently break image loading.
- ComfyUI-specific: no per-request seed/cfg/steps override (fixed defaults + random seed only); no per-request workflow selection (one server-wide `COMFYUI_WORKFLOW`); the two example workflow templates (`sdxl.json`, `flux.json`) reference specific model filenames that must exist in whichever ComfyUI installation is actually used — see `docs/COMFYUI_SETUP.md`.
- Hugging Face-specific: no per-request seed override (random seed only, same as ComfyUI); `num_inference_steps`/`guidance_scale`/`scheduler` aren't exposed (each model's own defaults apply); no automatic retry on a 503 "model is loading" cold-start response — see `docs/HUGGINGFACE_PROVIDER.md#limitations`.
- Frontend lint: 26 pre-existing errors, 3 pre-existing warnings, repo-wide (`chat.service.ts`, `use-login.ts`, `user-auth-form.tsx`, `use-projects.ts`, `model-card.tsx`, `stat-card.tsx`, `useHealth.ts`, `useModels.ts`, `useModelsList.ts`, `main.tsx`, `auth-provider.tsx`, `api.ts`, plus two React Compiler table-memoization warnings). None are in Content Studio's own files, including this sprint's new ones.
- 2 pre-existing frontend test failures in `search-provider.test.tsx` (command palette navigation, timeout-based) in addition to the 2 long-standing `user-auth-form.test.tsx` failures — both confirmed pre-existing, neither caused by this project's Content Studio work.
- No CI/CD auto-deploy exists yet (a standing risk since Sprint 1.1) — `deploy.sh` is manually triggered and requires sudo privileges this session does not have.
- Minor, deliberately out-of-scope items from the Content Studio UI investigation: `ProjectCard.tsx`/`QuickActions.tsx`/`ContentPipeline.tsx` use double-quoted `className` strings against the repo's `singleQuote: true` Prettier rule; `QuickActions` and `ContentPipeline` on the Content Studio dashboard render static, non-functional placeholder data that looks real; content-studio query keys are inconsistently constructed (inline arrays vs. one shared exported constant); `NewProjectDialog` defines its mutation inline instead of via a dedicated hook, unlike every other mutation in this feature.
- **`prettier --check` fails across essentially the entire `frontend/src/features/content-studio/` directory** (discovered this sprint) — including many files untouched by any Image Studio work, so this predates Sprint 4.3 and isn't caused by it. Not a repo-wide issue (the failing files are all under `content-studio/`); likely a formatting-convention drift specific to this feature. This sprint's own new files were formatted individually rather than running `prettier --write` repo-wide, to avoid an unrelated mass-reformat diff — the underlying drift is still unresolved.
- Content generation is slow on current hardware (~3 tokens/sec on `qwen2.5-coder:7b`, CPU-only, 11GB RAM VPS) — a full generation can take up to several minutes even with a warm model. Async/background generation is flagged as a likely future need but out of scope until its own sprint (see ADR-0005).
- `CONTRIBUTING.md` and `DEVELOPMENT.md` are incomplete (both cut off mid-instruction) — flagged during ACF planning, not yet fixed.
- A pre-existing `tsx watch` process (unrelated to this project's work, PID 207157 as of Sprint 3 Phase 1) was noticed during port investigation — not bound to any active port and not interfered with, but worth the user's attention if it's stale.

---

## Pointers

- CRM Foundation reference: `tasks/completed/sprint-1-crm-foundation.md` and `.claude/decisions/ADR-0018-crm-foundation.md` (shared-workspace/no-ownership rationale; why `LeadActivity` is one table not two; why status changes await their activity write instead of firing-and-forgetting; why the AI-analysis/execution-log models exist unused; why service-facing routes were deferred out of Sprint 1) — also see the "CRM Foundation Architecture" section in `docs/ARCHITECTURE.md` and the approved TDD at `/home/eyancantimbuhan/.claude/plans/project-ai-sales-clever-dijkstra.md`.
- CRM Automation Integration Contract reference (Sprint 2): `tasks/completed/sprint-2-automation-integration-contract.md` and `.claude/decisions/ADR-0019-automation-integration-contract.md` (webhook vs. service auth design, retry/timeout/idempotency/versioning policy, and rejected alternatives — including why `AUTOMATION_SERVICE_API_KEY`/`AUTOMATION_WEBHOOK_SIGNING_SECRET` deliberately don't use `requireEnv()`) — also see the "Sprint 2 — Automation Integration Contract" subsection under `docs/ARCHITECTURE.md`'s CRM Foundation Architecture section.
- Finance Management reference: `tasks/completed/sprint-A-finance-management.md` and `.claude/decisions/ADR-0013-finance-management-foundation.md` (Decimal money end-to-end; the lazy on-access recurrence-generation algorithm and why no scheduler was introduced; the Recharts single-slice Pie chart bug and its plain-SVG fix).
- AI Video Editing Pipeline reference: `tasks/completed/sprint-7-2-4-whisper-transcription.md` (Milestone 4, AI Transcription & Subtitle Generation), `tasks/completed/sprint-7-2-3-execution-engine.md` (Milestone 3, Execution Engine + Core FFmpeg Operations), `tasks/completed/sprint-7-2-2-workflow-planner.md` (Milestone 2, Workflow Planner), and `tasks/completed/sprint-7-2-1-source-ingestion.md` (Milestone 1, Source Ingestion) — the full architecture proposal covering the planner/execution engine/FFmpeg/Whisper/CV milestones was reviewed and approved before implementation began, but isn't itself a checked-in doc — these sprint logs and PROJECT_STATE.md are its record.
- MCP Foundation reference: `tasks/completed/sprint-7-1-mcp-foundation.md` and `.claude/decisions/ADR-0012-mcp-foundation.md` (registry pattern choice; why connectors never decrypt credentials; why business logic stays in services; how a future real provider, starting with Canva, should integrate) — also see the "MCP Foundation Architecture" section in `docs/ARCHITECTURE.md`.
- Completed sprints: `tasks/completed/` (most recent: `sprint-2-automation-integration-contract.md`; before that, `sprint-1-crm-foundation.md`; before that, `sprint-A-finance-management.md`; before that, `sprint-7-2-4-whisper-transcription.md`; before that, `sprint-7-2-3-execution-engine.md`, `sprint-7-2-2-workflow-planner.md`, `sprint-7-2-1-source-ingestion.md`, `sprint-7-1-mcp-foundation.md`, `sprint-6-6-production-dashboard.md`; also `sprint-6-5-analytics-foundation.md`, `sprint-6-4-publishing-pipeline.md`, `sprint-6-3-creative-review-workspace.md`, `sprint-6-2-ai-video-studio.md`, `sprint-6-1-brand-kits.md`, `sprint-5-asset-library-qa.md`, `sprint-4-4-huggingface-provider.md`, `sprint-4-3-comfyui-provider.md`, `sprint-4-2-phase-1-gemini-provider.md`, `sprint-4-2-phase-0-production-readiness.md`, `sprint-4-1-ai-image-studio-backend.md`, `sprint-3-5-security-hardening.md`, and `sprint-3-prompt-library.md`, which includes the earlier production incident record)
- Sprint 6 is fully complete (all six phases). Sprint 7.1 (MCP Foundation) is complete. Sprint 7.2 (AI Video Editing Pipeline) is active — Milestones 1-4 (Source Ingestion, Workflow Planner, Execution Engine + Core FFmpeg Operations, AI Transcription & Subtitle Generation) complete, Milestone 5 (computer-vision operations) not yet started or approved.
- Asset Library & QA reference: `docs/ASSET_LIBRARY.md` (data model, aggregation mechanics, how to add a future asset type — now including the real `BRAND_KIT` and `VIDEO` examples — the Creative Review Workspace's comments/annotations/assignment/timeline mechanics, the Publishing Pipeline's independent lifecycle/provider-registry/timeline-reuse mechanics, the Analytics Foundation's reuse-first/fire-and-forget mechanics, the Production Dashboard's shared-component/dataviz mechanics, and client-side-only export decision), `.claude/decisions/ADR-0008-asset-library-polymorphic-review-versioning.md` (why two additive tables instead of a unified `Asset` table), `.claude/decisions/ADR-0009-creative-review-workspace.md` (unified comments/annotations table; why review assignment is informational only), `.claude/decisions/ADR-0010-publishing-pipeline.md` (why publishing is independent of QA; why the provider registry mirrors `ImageProviderFactory`; why scheduling has no background execution yet), and `.claude/decisions/ADR-0011-analytics-foundation.md` (why a separate `AnalyticsEvent` table instead of reusing `AssetReviewEvent`; why the analytics write is genuinely unawaited; why no new RBAC gate)
- Brand Kits reference: `tasks/completed/sprint-6-1-brand-kits.md`
- AI Video Studio reference: `tasks/completed/sprint-6-2-ai-video-studio.md`
- Creative Review Workspace reference: `tasks/completed/sprint-6-3-creative-review-workspace.md`
- Publishing Pipeline reference: `tasks/completed/sprint-6-4-publishing-pipeline.md`
- Analytics Foundation reference: `tasks/completed/sprint-6-5-analytics-foundation.md`
- Production Dashboard reference: `tasks/completed/sprint-6-6-production-dashboard.md`
- Single-text-provider rationale (why Video Studio's text kinds have no provider registry): `.claude/decisions/ADR-0001-single-ai-provider-no-gateway.md`
- ComfyUI provider reference: `docs/COMFYUI_PROVIDER.md` (architecture, workflow templates, error handling) and `docs/COMFYUI_SETUP.md` (installation, configuration, nginx gap, troubleshooting)
- Hugging Face provider reference: `docs/HUGGINGFACE_PROVIDER.md` (architecture, model configuration, error handling) and `docs/HUGGINGFACE_SETUP.md` (API key setup, configuration, troubleshooting)
- Sprint 4.2 plan (provider order now superseded, rest still accurate): `tasks/backlog/sprint-4-2-provider-integration.md` (Sprint 4 readiness precursor: `tasks/backlog/sprint-4-readiness.md`)
- Sprint log template: `tasks/templates/sprint-log-template.md`
- Architecture decisions: `.claude/decisions/ADR-0001` through `ADR-0019`
- Operations playbook: `.claude/engineering/10_OPERATIONS.md` (updated with reverse-proxy timeout and Ollama warm-up requirements)
- Long-term direction: `docs/product/02_ROADMAP.md`
- Architecture overview: `.claude/context/repository-map.md`
- Portfolio log: `PORTFOLIO.md`
