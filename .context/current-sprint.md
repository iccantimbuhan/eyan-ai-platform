# Current Sprint

_Rewrite this file completely whenever `PROJECT_STATE.md`'s top entry changes. Current state only — never accumulate history here. Full history: `PROJECT_STATE.md`, then `tasks/completed/`._

**Restaurant Operations Platform — Sprint 1.3 (Restaurant Product Foundation) — implemented, tested against the isolated dev database, NOT yet deployed to production.** Completes the product-knowledge layer Inventory (Sprint 2+) will need but must never own itself: `Ingredient`, `IngredientCategory`, `Supplier`, `Unit`, `Recipe`, `RecipeIngredient` — all Restaurant-scoped, following the same ownership split Menu already established (ADR-0037). Full CRUD for all six, restaurant-scoped tenant-access guards (`requireUnitAccess`/`requireIngredientCategoryAccess`/`requireSupplierAccess`/`requireIngredientAccess`/`requireRecipeAccess`/`requireRecipeIngredientAccess`, sharing one `createRestaurantScopedAccessGuard` factory), and defense-in-depth scope-mismatch checks on every client-supplied foreign key (`ingredientCategoryId`, `supplierIds`, `menuItemId`, `ingredientId`/`unitId`), mirroring Sprint 1.1's `MenuCategoryMismatchError` pattern. An Ingredient's linked Suppliers are written as a full-set replace on the same create/update call (`supplierIds: string[]`), mirroring `roles.service.ts`'s `updateRolePermissions` pattern. Recipe is data-only this sprint — no stock deduction, no Inventory.

Domain model validated against real data from both pilot restaurants (Burger's Ink's supplier order sheet + kitchen ingredient list, Topo Gigio Pizzeria's ingredient list) before implementation, per this sprint's explicit "stop and review if a gap is found" instruction — no gap was found. The real data directly evidenced the many-to-many Ingredient↔Supplier relationship: Burger's Ink's own order sheet lists "oil" under two different suppliers (J.Calleja and Schembri Ltd).

62 new backend tests (6 resource services, 6 new middleware guards via a shared test generator); full backend suite 1024/1024 passing. Frontend: 6 new resource pages (Units, Ingredient Categories, Suppliers, Ingredients, Recipes — the last with an ingredient-line management dialog) joining Restaurants/Branches/Menu Categories/Menu Items/Staff under the Restaurant Operations nav group; 421/421 tests passing, clean typecheck/build/lint. Live end-to-end smoke test against the dev database using the real Burger's Ink/Topo Gigio data confirmed: multi-supplier ingredient creation, recipe + recipe-ingredient creation (the "Ultimate Cheese Burger, 20g Lettuce" example), duplicate-ingredient-on-recipe correctly rejected (409), and cross-tenant scope-mismatch defenses correctly rejected a Topo Gigio ingredient/supplier being attached to a Burger's Ink recipe/ingredient (400) — all test data cleaned up afterward. See `restaurant.md`, ADR-0037.

**Known gap, inherited unchanged from Sprint 1.2, deferred to Sprint 2**: Branch-scoped-only staff (Cashier/Kitchen/Inventory Staff with no Restaurant/Organization membership) cannot reach Menu *or* the new Ingredient/Recipe/Supplier/Unit routes — all are gated by `requireRestaurantAccess`/its Sprint 1.3 siblings, none of which consider `BranchMember`. Fixing this needs a read/write access split, out of scope for this sprint (ADR-0036, ADR-0037).

**Also complete — Restaurant Operations Sprint 0 (Tenancy Foundation), deployed and verified in production.** `Organization → Restaurant → Branch`, Module Registry, TeamSwitcher. See ADR-0025, ADR-0026, ADR-0035.

**Also complete — Restaurant Operations Sprint 1.1 (Master Data CRUD) and Sprint 1.2 (Customer Authorization & Staff Management), validated against the dev database, NOT yet deployed to production** (same pending-deploy status as Sprint 1.3 above — all three can promote together in one `deploy.sh` run). Restaurant/Branch/Menu Category/Menu Item CRUD; `Restaurant Customer` platform Role, `TenantRole` enforcement, `BranchMember`, Staff Management.

**Also complete — Sprint 5.2 (Production Stabilization, unrelated track).** Fixed CRM AI qualification write-back to accept `"UNKNOWN"` as a legitimate value (see `crm.md`), fixed a missing `trust proxy` config, fixed a Slack/Email notification bug in the automation hub's Workflow 4.

**Next up:**
- **Deploy Sprint 1.1 + 1.2 + 1.3 to production** — migrations + `bootstrapPlatform()` promote through the same `deploy.sh` pipeline; a normal deploy is all that's needed once approved.
- **Restaurant Operations Sprint 2** — Inventory/Purchases/Expenses (Branch-scoped), consuming the Ingredient/Recipe catalog this sprint built; resolve the Branch-scoped-staff Menu/Ingredient-access gap above as part of this work — see `restaurant.md`.
- **AI Core Phase 2** — migrate the remaining call sites (Chat, Video, Content) to invoke AI Core Capabilities instead of calling providers directly, one call site at a time, no breaking changes. CRM is already migrated (Phase 3 — see `ai-core.md` and `crm.md`).
